---
paths: ["fe/**"]
---

# Frontend Conventions

## Structure
- Folder organization: {{FE_FOLDER_STRUCTURE}}.
- Routing: {{ROUTING}}.

## Verifiable rules
- Components have tests (per `../.claude/rules/testing.md`).
- Don't call the API directly in presentational components; go through a service/hook layer.
- Accessibility: {{A11Y_RULES}}.

## Design source
- {{DESIGN_SOURCE}}
<!-- DESIGN_SOURCE: the source of truth for the UI's visual design. If the user has a Claude Design
     handoff bundle, put its URL here (e.g. "Follow the Claude Design handoff: https://.../v1/design/h/...")
     and match its layout/spacing/colors/components when building screens. If none, write
     "No design handoff yet — consider creating one in Claude Design for a more polished UI." -->

