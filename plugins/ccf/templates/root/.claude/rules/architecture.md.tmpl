# Architecture

## Layering & boundaries
{{LAYERING}}
<!-- Example: presentation → application/service → domain → infrastructure -->

## Dependency direction
- Dependencies flow one way only: {{DEPENDENCY_DIRECTION}}.
- Business logic does NOT depend on frameworks/IO directly.

## Design patterns
- Backend: {{BE_PATTERN}}.
- Frontend: {{FE_PATTERN}}.

## Where things go
{{WHERE_THINGS_GO}}
<!-- Example: API handlers in `be/src/api/handlers/`; domain models in `be/src/domain/` -->

## Verifiable rules
- Each module has one clear responsibility (Single Responsibility).
- No circular imports between layers.
