# Implementation Plan — {{PROJECT_NAME}}

> **Execution rule: STRICTLY SEQUENTIAL + VERTICAL SLICES.** Do exactly one task at a time, in order.
> Each task is a thin tracer-bullet that crosses all the layers it touches (DB + service + UI) — NOT a horizontal "all-DB-then-all-API" phase — so integration is proven early.
> Do not start task N+1 until task N's **gate is GREEN** (implemented + tested + checked via `/ccf:check`).
> The `in-progress`/`in-review` status is read by the session-start hook to re-load context after compact — keep status up to date.

## Milestones
{{MILESTONE_LIST}}

> Status: `todo` / `in-progress` / `in-review` / `done` / `blocked`. Lifecycle: `todo → in-progress → in-review → done`. `ccf-implementer` marks `in-review` when code+test are complete; only `/ccf:updatespec` writes `done` after `/ccf:check` + `/code-review` pass.
> Write the status as a **bare word** — no `**bold**` around it. Emphasis carries no information and the status is matched as a whole word.
> Per-task detail in `task-NNN-*.md`.

> **Keep this file to the CURRENT iteration.** When every task of an iteration is `done`, `/ccf:updatespec` moves its sections verbatim into `.claude/plan/ARCHIVE.md` and `git mv`s its `task-NNN-*.md` files into `.claude/plan/archive/`. Two reasons, pulling in opposite directions: a closed row left here is counted as live work by the session-start and Stop hooks, while DELETING the history would strip `ccf-spec-checker`'s premortem of the real past failures it anchors to. So archive, never delete.

<!-- The status guidance above sits BEFORE the first "## Origin" heading on purpose: lib/archive.mjs#parseIterations
     groups everything from one "## Origin" heading to the next as ONE retirable iteration, and content before the
     FIRST such heading belongs to no iteration. Placing this guidance AFTER "## Origin" would make the very first
     retirement cut it into ARCHIVE.md along with the closed iteration. Keep it here, never below "## Origin". -->

## Origin: {{ITERATION_NAME}}
<!-- ITERATION_NAME: name of the first iteration /ccf:init plans -->

## Task backlog (in execution order)
| # | Slice | Layers | Gate (tests green) | Depends on | Status |
|---|-------|--------|--------------------|-----------|--------|
| 001 | {{TASK_001}} | {{LAYERS_001}} | {{GATE_001}} | — | todo |
| 002 | {{TASK_002}} | {{LAYERS_002}} | {{GATE_002}} | 001 | todo |
{{MORE_TASKS}}
