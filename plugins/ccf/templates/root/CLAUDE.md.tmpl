# {{PROJECT_NAME}}

> Managed by **CCF (Claude Context First)**. Workflow: Explore → Plan → Implement → Commit.
> **STRICTLY SEQUENTIAL**: one task at a time, no parallel feature development.
> Ground every design decision in Context7 + Microsoft Learn.
> Keep this spec always fresh with `/ccf:updatespec`.

## What this is
{{ONE_PARAGRAPH_SYSTEM_DESCRIPTION}}

## Repo layout (monorepo)
- The root folder holds `CLAUDE.md`, `.claude/`, docker files, CI/CD files. **git init at the root**, not in sub-folders.
{{MONOREPO_LAYOUT}}
<!-- Fullstack example:
- `be/` — backend (has its own CLAUDE.md + .claude/).
- `fe/` — frontend (has its own CLAUDE.md + .claude/).
-->

## Rules (imported — keep this file < 200 lines; detail lives in .claude/rules/)
@.claude/rules/tech-stack.md
@.claude/rules/architecture.md
@.claude/rules/coding-conventions.md
@.claude/rules/logging.md
@.claude/rules/testing.md
@.claude/rules/error-handling.md
@.claude/rules/debugging.md
@.claude/rules/tooling.md
@.claude/rules/git-workflow.md

## Current plan
See `.claude/plan/PLAN.md` for the sequential backlog of the CURRENT iteration. Execute **one task at a time**, in order; do not start task N+1 until task N is implemented + tested + checked. Closed iterations move to `.claude/plan/ARCHIVE.md` (task files to `.claude/plan/archive/`), so keep this section about the work in flight — not a running history. The archive is what `ccf-spec-checker`'s premortem anchors to, so archive it, never delete it.
