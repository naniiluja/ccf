# CCF — Claude Context First (plugin source)

> Managed by **CCF**. This project IS the source code of the CCF plugin — not an app with a DB/API/frontend.
> **STRICTLY SEQUENTIAL**: one change at a time, no parallel work on multiple things.
> Ground every decision about Claude Code's schema/contract in the official docs (via Context7) before writing.
> Keep this spec always fresh with `/ccf:ccf-updatespec`.

## What this is
CCF is a **Claude Code plugin** that imposes a context-first, spec-driven, strictly sequential workflow. It has NO application runtime (no server, DB, API, UI). The entire "product" is the artifacts Claude Code loads: commands (markdown prompts), agents/subagents (markdown), hooks (`.mjs` scripts run by `node`), templates for `/ccf-init` to instantiate, plus distribution manifests. Users install via a marketplace, then run `/ccf:ccf-*`.

## Repo layout
- **git init at the root** (`D:/projects/ccf`). The root holds `CLAUDE.md`, `.claude/`, `package.json`, `tsconfig.json`, `bin/`, `README.md`, `LICENSE`, `.claude-plugin/marketplace.json`.
- `plugins/ccf/` — the plugin itself. `.claude-plugin/plugin.json` is the manifest (ONLY the manifest goes in `.claude-plugin/`); the component directories live at the **plugin root**:
  - `commands/*.md` — 6 slash commands (`ccf-init`, `ccf-plan`, `ccf-check`, `ccf-fix`, `ccf-updatespec`, `ccf-cook`).
  - `agents/*.md` — 6 subagents (`ccf-codebase-analyzer`, `ccf-best-practice-researcher`, `ccf-implementer`, `ccf-spec-writer`, `ccf-spec-checker`, `ccf-debugger`).
  - `skills/grill-me/SKILL.md` — 1 internal skill: the shared requirements-interview engine invoked by `ccf-plan`/`ccf-fix`/`ccf-init` (`user-invocable: false`; hidden from the `/` menu).
  - `hooks/*.mjs` + `hooks/hooks.json` + `hooks/lib/` — 9 hooks (plan-mode-guard, plan-review-gate, session-start, updatespec-nudge — advisory Stop nudges, auto-verify — an opt-in `--auto-verify` Stop hook that BLOCKS via `decision:"block"` to drive the verify chain when a task is in-review and code changed this session, context-guard — warns/optionally hard-blocks on `UserPromptSubmit` when context enters the degrade zone — agent-rules-inject, which on `SubagentStart` injects the project coding rules + active output style's coding rules into spawned `ccf-implementer` subagents, explore-guide-inject, which on `SubagentStart` (matcher `Explore`) injects a language-agnostic LSP/Grep/Glob exploration directive into the built-in `Explore` subagent, and implementer-verify-gate — an opt-in `--enforce-tests` `SubagentStop` hook (matcher `ccf-implementer`) that BLOCKS via `decision:"block"` when a spawned implementer's final message carries no `TEST-RESULT:` evidence) sharing `lib/io.mjs` (+ `lib/freshness.mjs`, `lib/plan.mjs`, `lib/context-usage.mjs`, `lib/review-trace.mjs`, `lib/git-trace.mjs`, `lib/verify-trace.mjs`, `lib/verify-chain.mjs`, `lib/output-style.mjs`, `lib/explore-guide.mjs`, `lib/implementer-verify.mjs`).
  - `templates/{root,backend,frontend}/**` — `*.tmpl` files with `{{...}}` placeholders for `/ccf-init` to instantiate.
  - `.mcp.json` — bundles 2 remote MCP servers (microsoft-learn, context7).
- `bin/ccf-bootstrap.mjs` — the npx entry; only shells out to the `claude plugin` CLI, writes no files itself.

## Core invariants (read before editing)
- Hooks are **no-build, no-dependency, Windows-clean** `.mjs` run directly with `node` (Node ≥ 18). Do NOT add a dependency, do NOT add a build step. See `@.claude/rules/hooks.md`.
- Components (command/agent/template) are **markdown prompts**, not executable code. Editing the content = changing Claude's behavior. See `@.claude/rules/components.md`.
- `${CLAUDE_PLUGIN_ROOT}` only expands in `hooks[].command` and `mcpServers` — NOT in markdown frontmatter.
- Every `CLAUDE.md` (including ones CCF generates for other projects) must be < 200 lines, pushing detail into `.claude/rules/*` via `@import` (max depth 5).

## Rules (imported — keep this file < 200 lines)
@.claude/rules/architecture.md
@.claude/rules/components.md
@.claude/rules/hooks.md
@.claude/rules/coding-conventions.md
@.claude/rules/testing.md
@.claude/rules/tooling.md
@.claude/rules/git-workflow.md

## Current plan
In `.claude/plan/PLAN.md` — lead iteration **advisor+goal docs + SubagentStop verify-gate** (033–034 **done**, v0.6.0; `034a` **done** by user acceptance — live payload UN-OBSERVED, so the hook stays `--enforce-tests` default-OFF until a real `SubagentStop` payload is captured): a grounded audit of `/ccf-plan` vs the community repo `shanraisshan/claude-code-best-practice` found it meets 6/7 planning criteria (only cross-model review was missing). **033** adds OPTIONAL official runtime commands — `/advisor` (cross-model review) to `ccf-plan` step 6 + `ccf-check`, and `/goal` (conditional stop) to `ccf-cook` §8 — recorded in `tooling.md`; they NEVER replace the mandatory `ccf-spec-checker`/stop-on-red gates (pure prose, count unchanged). **034** adds the 9th hook `implementer-verify-gate` — an OPT-IN (`--enforce-tests`, default OFF) `SubagentStop` hook (matcher `ccf-implementer`) that BLOCKS a spawned implementer from stopping until its final message carries `TEST-RESULT:` evidence (a `ccf-implementer` Return-format line pinned to `TEST-RESULT: <cmd> → <counts>` or `TEST-RESULT: n/a (no test surface)` for prose-only); pure `lib/implementer-verify.mjs` (`resolveLastMessage`/`implementerReportedTests`/`shouldBlockImplementerStop`/`extractLastAssistantText`) + `io.mjs#blockSubagentStop`. It reads the DOCUMENTED `last_assistant_message` field FIRST (transcript fallback only when absent) because SubagentStop's `stop_hook_active` loop-guard + `transcript_path` are UNCONFIRMED by docs → `034a` must OBSERVE the real payload (reload + `--enforce-tests`) before the toggle is trusted; if `stop_hook_active` is absent the toggle stays permanently disabled. Rejected after grounding: `grill-me context:fork` (AskUserQuestion is NOT available to subagents), `<important if>` conditional memory tags (do not exist — `paths:` is the real mechanism), git worktrees + parallel agent-teams (violate the strictly-sequential law). Net count is now **6 cmd / 6 agent / 9 hook / 1 skill**. Prior iteration **retire-ccf-test + rename-cook**: the standalone `/ccf:ccf-test` command was REMOVED (its EP/BVA/decision-table matrix design is redundant — `ccf-implementer` already writes the matrix tests failing-first during implement, so a separate command duplicated that; every `/ccf:ccf-test` reference in `ccf-cook`/`ccf-check`/`ccf-plan`/`ccf-implementer`/`verify-chain.mjs`/`auto-verify.mjs`/rules/READMEs was rewritten to "run the project's test command to confirm the matrix tests pass" where the discipline is on), and `/cook` was RENAMED to `/ccf:ccf-cook` (`commands/cook.md` → `commands/ccf-cook.md`) so it follows the `ccf-*` naming of every other command. `verify-chain.mjs#buildVerifyReason` still gates the test-suite step on `readDisciplineOn`, only the step text changed (its unit tests updated to assert the reworded step, not a command name). Net count is now **6 cmd / 6 agent / 8 hook / 1 skill**. Prior iteration **cook-orchestrator + nested-spawn-guard** (task 031 **in-review**, lead task, no predecessor; task 032 the `/ccf-cook` orchestrator command **in-review**; `032a` a deliberate hanging live-verify `todo`): task 032 adds `commands/ccf-cook.md` — a sequential implement loop (`ccf-implementer`, `sonnet` alias) then a batch-verify pass (read-only `ccf-spec-checker` + `/code-review` in parallel, then `/simplify` alone, re-gate, `/ccf:ccf-updatespec`), mutually exclusive with `auto-verify.mjs --auto-verify`. Task 031 re-grounds via Context7 `/websites/code_claude` `/sub-agents` that a subagent CAN spawn nested subagents up to a fixed depth of 5 (the `Agent`/`Task` tool is inherited like any other tool, NOT blocked by default) and fixes a WRONG spec claim in `architecture.md`/`components.md` that asserted the opposite ("session-state/spawn tools never reach a subagent"); it then adds `disallowedTools: Agent, Task` to all 6 agents — `ccf-implementer` moves from OMIT `tools` to `disallowedTools: Agent, Task` (still inherit-all-minus-spawn: Write/Edit/MCP/Skill unchanged), the 5 read-only agents extend their existing `disallowedTools: Write, Edit, NotebookEdit` denylist with `, Agent, Task` — each agent body also states the leaf-agent invariant in prose (defense-in-depth, `plan-mode-guard ↔ ccf-plan` pattern). This is a pure prompt/frontmatter change (no hook parses agent frontmatter); count stays **6 cmd / 6 agent / 8 hook / 1 skill** for task 031 (task 032's new orchestrator command is what bumps the command count, not this task). Prior iteration **agent-effort** (tasks 029 **in-review** + `029a` a deliberate hanging post-reload live-verify still `todo`): add the `effort` frontmatter field to all 6 subagents (a field plugin-shipped agents support since Claude Code 2.1.78 — changelog) along the SAME cost/difficulty axis as `model` — `ccf-codebase-analyzer`=`low`, `ccf-implementer`/`ccf-best-practice-researcher`/`ccf-spec-writer`=`medium`, `ccf-spec-checker`/`ccf-debugger`=`high`. `effort` is ORTHOGONAL to `model` (it tunes reasoning DEPTH within the chosen model, does NOT change it); omit→inherit session, an unsupported level falls back to the highest supported ≤ it. Grounded (`/websites/code_claude` `/sub-agents`+`/costs`+`/model-config`+changelog): the trigger was the UI not showing a model label on a spawned agent — but `model:` was already correct and in effect, so this iteration adds the missing `effort` axis (frontmatter + prose in `components.md`/`architecture.md`) rather than touching `model`. NO hook parses agent frontmatter, so this is a pure prompt/doc change; count stays **6 cmd / 6 agent / 8 hook / 1 skill**; `029a` (cache-not-repo → effort only takes effect after a plugin reload) is a HARD non-waivable observation gate. Prior iteration **right-size-slices + auto-verify-chain** (tasks 027–028 **done** [027 = the right-size-slices rule, 028 = the hook]; `028a` a deliberate hanging live-verify still `todo` — needs a plugin reload + `--auto-verify` to OBSERVE the real behavior, not waivable without observation): a new OPT-IN `Stop` hook `auto-verify` (toggled by adding `--auto-verify` to its `hooks.json` command, like context-guard's `--hard-block`) uses the grounded "ralph loop" (`decision:"block"` + `reason`) to drive the main loop through the verify chain (`/ccf:ccf-check` → `/code-review` → run the project's test command if the test discipline is on → `/ccf:ccf-updatespec` only when both come back clean) when there is an in-review task AND this session changed code AND no `ccf-spec-checker` review has run yet. Pure decisions live in `lib/verify-chain.mjs` (`shouldDriveVerify` = `enabled && !stopHookActive && hasInReviewTask && editedCodeThisSession && !checkAlreadyRan`, `readDisciplineOn`, `buildVerifyReason`), reusing the tested `plan.mjs#findActiveTask` / `verify-trace.mjs#readTranscriptSignals` / `review-trace.mjs#hasSpecCheckerReview`; the new `io.mjs#blockStop` is the Stop block channel. The `Stop` array now holds TWO hooks: `updatespec-nudge` (advisory) + `auto-verify` (opt-in BLOCKING). This bumps the count to **6 cmd / 6 agent / 8 hook / 1 skill**. Prior iteration **explore-guide-inject** (tasks 025–026; done — docs/count sync): a new `SubagentStart` hook `explore-guide-inject` (matcher `"Explore"`) injects a short, language-agnostic, LSP-conditional exploration directive into the built-in `Explore` subagent so it prefers SEMANTIC navigation (the `LSP` tool — `workspaceSymbol`/`goToDefinition`/`findReferences`/`documentSymbol`) + ripgrep `Grep` + `Glob` over reading whole files; the `SubagentStart` array now holds TWO hooks (agent-rules-inject = no matcher + internal `WRITER_AGENTS` filter; explore-guide-inject = matcher `"Explore"`, no internal filter); content is the pure `lib/explore-guide.mjs`, best-effort never-block. This bumps the count to **6 cmd / 6 agent / 7 hook / 1 skill**. Prior iteration **context-guard compact-boundary** (task 024 todo, `024a` a deliberate hanging live-verify; INDEPENDENT of 022/023 — disjoint files): fix the UX bug where context-guard kept warning "over threshold" right AFTER `/compact`. Root cause (grounded `/websites/code_claude`): `/compact` writes a `{type:"system",subtype:"compact_boundary"}` line into the SAME transcript then continues; `readContextUsage` walks back and, before any post-compact assistant turn exists, reads the pre-compress assistant line above the boundary (stale, large). Fix = on the reverse walk, hitting `compact_boundary` BEFORE an assistant-with-usage line → `return null` → hook stays silent (existing `if(!usage) process.exit(0)`). Predicate is NARROW (`type==="system" && subtype==="compact_boundary"`, no `compact_metadata` fallback — too broad, contradicts the Bug #4 lesson). `024a` captures the REAL boundary line on the user's harness as a fixture (premortem mitigation); added no command/agent/hook/skill. Prior in-review iteration **subagent-tool-inheritance** (tasks 022–023, in-review; `022a` is a deliberate hanging post-reload live-spawn verify): CCF subagents now reach the host project's ARBITRARY MCP (oracle, chrome-devtools, …) + project skills by INHERITANCE, not a `tools` allowlist — the writer `ccf-implementer` OMITS `tools` (inherit-all incl. all project MCP + Skill); the 5 read-only agents use `disallowedTools: Write, Edit, NotebookEdit` (inherit-all-minus-file-writes). Grounded (`code.claude.com/docs/en/sub-agents` + `/en/tools-reference`): a `tools` allowlist BLOCKS unlisted MCP + Skill and `mcpServers`/`permissionMode`/`hooks` are "Ignored for plugin subagents", so inheritance is the only mechanism; safety = file-write denial + per-call permission prompts + the leaf-agent denylist (nested spawn IS possible by default — blocked deterministically by 031's `disallowedTools: Agent, Task`); an inherited MCP tool may be lazily loaded → use `ToolSearch` before calling. Shipped on the 6 agent frontmatter+body (022) then synced to rules/READMEs/CLAUDE.md (023); COMMAND `allowed-tools` stays least-privilege (main-loop scope); added no command/agent/hook/skill. Prior closed iteration **premortem-reviewer** (tasks 020–021, done — awaiting commit): `ccf-spec-checker`'s plan-review now adds a **premortem / prospective-failure lens** (assume the plan shipped and failed; list 2–4 likely failure modes, each anchored to a real past iteration in `PLAN.md` "Closed" + project memory, each with one preventing plan change) — shipped in `ccf-spec-checker.md` + `ccf-plan.md` step 6 + `ccf-init.md`'s A4 gate (020), then synced into the rules/READMEs/CLAUDE.md (021); the plan-review-gate MECHANISM is unchanged (still keys on `ccf-spec-checker`, still 1 review), added no command/agent/hook/skill. Prior still-in-review iteration — **output-style-propagation** (tasks 017–019, in-review): output styles modify the MAIN loop only and are NOT inherited by subagents ([#8395](https://github.com/anthropics/claude-code/issues/8395)), so spawned `ccf-implementer`s violated coding rules; fix = a new `SubagentStart` hook `agent-rules-inject` that injects the `.claude/rules` + active-style coding rules (persona excluded) into the writer subagent, the `ccf-implementer` body restating them as backup (the 6th hook at that time; explore-guide-inject later made it the 7th). Latest closed iteration — **memory-protocol-align** (tasks 015–016: aligned `/ccf:ccf-updatespec`'s memory instructions with Claude Code's real Memory mechanism — MEMORY.md 200-line/25KB pure index, `[[name]]` links, point-in-time/verify, Auto-Memory note, `feedback`-strongest; mirrored to template + 3 READMEs). Prior iterations also closed: context-guard hook (012–014, v0.3.3), plan-status-sync (010–011, v0.3.1), test-discipline opt-in (005–009, v0.3.0), best-practice integration (001–004, v0.2.0). **Task-status lifecycle**: `todo → in-progress → in-review → done` — `ccf-implementer` reaches `in-review`; only `/ccf:ccf-updatespec` writes `done` after `/ccf:ccf-check` + `/code-review` pass. When you need a new change, enter plan mode and run `/ccf:ccf-plan`; execute one task at a time in a fresh session via `ccf-implementer`, gate GREEN before the next.
