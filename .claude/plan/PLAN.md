# Implementation Plan — CCF (multi-iteration backlog; lead iteration at top)

> **Execution rule: STRICTLY SEQUENTIAL.** Do exactly one task at a time, in order.
> These tasks are slices sequenced for serial execution (thinnest → richest). Each `Depends on` = the prior task in the queue (serial law), unless a real data dependency is noted.
> Do not start task N+1 until task N's **gate is GREEN** (implemented + tested + checked).
> The `in-progress`/`in-review` status is read by the session-start hook to re-load context after compact — keep status up to date.

> **Scope of this file: the CURRENT iteration only.** Closed iterations and their postmortems live in
> `.claude/plan/ARCHIVE.md`; their task files live in `.claude/plan/archive/`. Keep it that way — a
> closed row left here is counted as live work by `lib/plan.mjs` (`findActiveTask` / `findNonDoneTasks`)
> and by the Stop nudge. When an iteration closes, move its `## Origin` / backlog / `## Closed`
> sections into `ARCHIVE.md` verbatim and `git mv` its task files into `archive/`.
> **Premortem note:** `ccf-spec-checker` and `/ccf:plan` step 6 anchor failure modes to real past
> iterations, so they must read `ARCHIVE.md` as well as this file.
## Origin — cc-2.1.220-realign (tasks 036–041) — IN-REVIEW (LEAD, v0.8.0, no predecessor)
Spec CCF được đối chiếu tài liệu lần cuối ở bản Claude Code 2.1.78, máy chạy 2.1.220. Rà changelog giữa hai bản: 3 chỗ spec nói sai, 1 lỗ hổng cơ chế, 1 nghi vấn hook chưa từng chạy, cộng 3 yêu cầu trực tiếp của người dùng (văn phong, chọn model, lời nhắc compact).

**Ba chỗ spec nói sai.** (1) Từ bản 2.1.163 Stop và SubagentStop ĐỀU nhận `hookSpecificOutput.additionalContext`; `hooks.md` và `io.mjs` nói ngược, nên `updatespec-nudge` chỉ nói được với người dùng còn model không nhận lời nhắc. (2) Độ sâu spawn agent con là VERSION-DEPENDENT: mặc định 3, là 5 ở v2.1.172 tới v2.1.216, là 1 ở v2.1.217/v2.1.218, đổi được bằng `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH`; spec cũ viết "fixed depth limit of five levels" nên sai cả con số lẫn chữ "fixed". (3) Từ 2.1.198 subagent chạy nền theo mặc định, đe dọa luật làm tuần tự; không có lever frontmatter ép đồng bộ, nên luật được cột bằng `run_in_background: false` ở 12 chỗ gọi trong 6 lệnh.

**Kết quả từng task.** **036** quy tắc văn phong 7 điểm (viết đúng một ngôn ngữ, chỉ giữ nguyên định danh, không em-dash, một câu một ý) vào 3 lệnh + `ccf-spec-writer` + `coding-conventions.md`, cộng bỏ đóng cứng model (frontmatter chỉ còn là MẶC ĐỊNH; `cook.md` đọc dòng `Model` từ file task, thiếu thì hỏi người dùng MỘT lần cho cả lượt; `grill-me` hỏi mức mặc định ở bước 2, ghi đè theo task ở bước 5). **037** sửa con số độ sâu + `run_in_background: false`. **038** sửa chú thích sai về Stop trong `io.mjs`, thêm `stopHookActive` làm chốt chống lặp cho SubagentStop, đổi tên `hasSpecCheckerReview` → `hasSpecCheckerSpawn` (đủ 9 nhóm) vì hàm chỉ chứng minh đã GỌI agent chứ không phải đã review XONG. **039** task điều tra, do VÒNG CHÍNH làm (6 agent CCF đều có `disallowedTools: Agent, Task` nên không gọi được agent con). **040** chia lại việc ở lời nhắc compact: hook lo phát hiện vượt ngưỡng và cấp dữ liệu, model lo diễn đạt và tự soạn câu `/compact` bằng ngôn ngữ người dùng. **041** `io.mjs#emitStopAdvisory` + cờ opt-in `--dual-channel-stop` (mặc định TẮT, không ghi vào `hooks.json`) + `io.test.mjs` là lưới an toàn ĐẦU TIÊN cho `io.mjs` + version 0.8.0.

**Task 039 kết luận nhánh MỘT, quan sát thật:** agent chạy nền trả `tool_result` là ack TỨC THÌ (~1072 ký tự, mở đầu `Async agent launched successfully.`), agent đồng bộ trả báo cáo 4463 tới 17895 ký tự; kết quả cuối của agent nền KHÔNG về qua `tool_result` mà qua bản ghi thông báo riêng; `is_error` là `undefined` ở MỌI ca nên KHÔNG dùng được làm dấu hiệu (dự tính ban đầu của kế hoạch đã SAI ở điểm này). Hàm kiểm tra "review đã xong" viết được bằng cách LOẠI mẫu ack, đã hoãn sang **task 042**.

**Ba lỗi thật đào ra NGOÀI kế hoạch.** (a) `readStdinJson` crash `exit 1` khi stdin là `"null"` (JSON hợp lệ nhưng không phải object → hook truy cập `input.prompt`), vi phạm chính bất biến mà `hooks.md` và `testing.md` đã khai; sống được vì 10 file test cũ KHÔNG file nào chạm `io.mjs`. (b) `shouldInject` khớp TUYỆT ĐỐI `ccf-implementer` trong khi tên agent quan sát được luôn có tiền tố `ccf:` (39 trên 39 lần trên 106 transcript, dạng không tiền tố 0 lần), tức `agent-rules-inject` có thể CHƯA TỪNG chạy và cơ chế của lần làm 017 tới 019 đã chết im lặng; nay so khớp phần sau dấu hai chấm (chặt, không khoan dung bừa). (c) cả cây `plugins/ccf/templates/**` bị bỏ sót ở lần rename 035, còn 12 chỗ mang tên lệnh cũ (`/ccf:ccf-check`, `/ccf-plan`, …), nghĩa là `/ccf:init` đang sinh spec cho dự án khác với tên lệnh KHÔNG TỒN TẠI; đã sửa hết, tên agent giữ nguyên.

**Review.** `ccf-spec-checker` ngữ cảnh sạch 2 vòng (vòng 1: 4 ❌ + 15 ⚠️; vòng 2: 2 ❌ + 11 ⚠️), tất cả đã tiếp thu và được xác minh ĐỘC LẬP (reviewer tự tái hiện crash trên mã cũ, so checksum `io.mjs` trước và sau khi chạy test, grep cả 6 lệnh). Lỗi nặng nhất vòng 1: `io.test.mjs` đột biến ghi THẲNG vào `io.mjs` thật, mà `try/finally` không chạy khi tiến trình bị ngắt bằng tín hiệu → có thể phát hành file mang `MUTATION-KILL-MARKER`; đã sửa thành `cpSync` sang thư mục tạm rồi đột biến BẢN SAO. `/simplify` 4 góc song song → 8 mục sửa, 4 mục cố ý bỏ (không xoá phép thử phá logic; không gộp `emitContext`; không thêm hook `PreToolUse` cưỡng chế `run_in_background`; không chuyển khối văn phong vào rule). **`/code-review` KHÔNG chạy được** trên harness này (skill `review` chỉ nhận pull request GitHub) → nhánh dự phòng `cook.md` bước 6: người dùng phải chạy tay.
Memory: [[ccf-plugin-runs-from-cache-not-repo]], [[subagentstop-shape-unconfirmed-read-last-assistant-message]], [[agent-type-carries-plugin-prefix]], [[background-agent-result-is-instant-ack]], [[slashcommand-tool-not-always-exposed]], [[test-must-fail-without-logic]].

## Task backlog — cc-2.1.220-realign (theo đúng thứ tự thực thi)
| # | Slice | Layers | Gate (tests green) | Depends on | Status |
|---|-------|--------|--------------------|-----------|--------|
| 036 | Quy tắc văn phong 7 điểm + bỏ đóng cứng model (frontmatter chỉ còn là mặc định) | 3 cmd + 1 agent + 1 skill + 1 rule | grep 5 chỗ phần A + `cook.md` hết đóng cứng + `grill-me` có câu hỏi model + `tsc` + `validate` + test nền | — | **done** |
| 037 | Con số độ sâu VERSION-DEPENDENT + `run_in_background: false` (12 chỗ) + dọn 12 tên lệnh cũ sót trong `templates/` | 6 cmd + 3 README + CLAUDE.md + 2 rule + 7 template | `rg` độ sâu 9→3 + grep cờ 12 chỗ + đếm từ khoá luật 107→107 + tên lệnh cũ trong `templates/` về 0 + `tsc` + test nền | 036 | **done** |
| 038 | Chú thích Stop + `stopHookActive` + đổi tên hàm đủ 9 nhóm | 3 lib + 3 hook + 2 test + 1 cmd + CLAUDE.md | test đỏ-rồi-xanh (21/1 → 22/0) + smoke gate 3 ca + smoke `plan-review-gate` 2 ca (bất biến) + `tsc` (lưới bắt import sai) | 037 | in-review |
| 039 | Task điều tra agent nền + chụp tên agent thật (VÒNG CHÍNH làm, KHÔNG phải implementer) | live, 0 file spec | có dữ liệu mẫu + kết luận + đã chọn nhánh MỘT | 038 | in-review |
| 040 | Hook lo phát hiện, model lo diễn đạt câu `/compact` | 1 lib + 1 hook + 1 test + 1 rule | test chống tái phạm (không chứa chuỗi tiếng Anh đóng cứng) + smoke 2 ca + `tsc` | 039 | in-review |
| 041 | `emitStopAdvisory` + cờ `--dual-channel-stop` + `io.test.mjs` (lưới đầu tiên cho `io.mjs`) + v0.8.0 | 3 lib + 1 lib mới + 1 test mới + 1 hook + 4 version + CLAUDE.md | 200 pass (190 khi task đóng, cộng 10 ca của vòng correctness) + phép thử phá logic + kiểm phạm vi `git diff io.mjs` + `shasum` bất biến + smoke hai chiều | 040 | in-review |

> Status: `todo` / `in-progress` / `in-review` / `done` / `blocked`. Lifecycle: `todo → in-progress → in-review → done` — `ccf-implementer` reaches `in-review`; only `/ccf:updatespec` writes `done` after `/ccf:check` + `/code-review` pass.
> **`/ccf:updatespec` ĐÃ CHẠY** (cuối phiên này, sau khi người dùng mở quyền ghi vùng `.claude/`), đã hoàn tất bước 4 (ghi vào `tooling.md`: giới hạn của skill `review`, hành vi quan sát được của `simplify` và của `/goal`) và bước 5 (2 ghi chú nhớ mới `user-wants-plain-language-and-model-choice` + `verify-by-reproducing-not-by-reading-reports`, cộng 3 ghi chú đã ghi trước đó), rồi ở bước 6 quyết định trạng thái **theo TỪNG task** chứ không gộp một cục:
> - **036 và 037 → `done`.** Hai task này thuần văn bản hướng dẫn, KHÔNG thêm hành vi runtime nào, nên cửa kiểm của chúng là grep cộng test cộng `tsc` và đã xanh, đã được ba vòng review xác minh độc lập. Không có gì để "quan sát thật", nên giữ chúng ở `in-review` sẽ là thận trọng hình thức chứ không phải thận trọng thật.
> - **038, 039, 040, 041 → giữ `in-review`.** Mỗi task này còn một cửa kiểm CHƯA quan sát: 038 thêm `stopHookActive` (hình dạng payload `SubagentStop` chưa quan sát); 039 chưa chụp được `agent_type` thật của payload `SubagentStart`; 040 chưa quan sát lời nhắc compact sau khi nạp lại plugin; 041 chưa quan sát trọn gói bốn công tắc.
>
> **Thay cho `/code-review` (không chạy được trên harness này), đã chạy một vòng review CORRECTNESS THUẦN từng dòng bằng `ccf-spec-checker` ngữ cảnh sạch.** Vòng đó bắt được **1 ❌ THẬT mà hai vòng review trước bỏ sót**: đường MẶC ĐỊNH của `updatespec-nudge.mjs` mất hoàn toàn lời khuyên HÀNH ĐỘNG ở cả ba nhánh (lời khuyên bị dồn vào `directive`, mà `directive` chỉ phát khi có cờ, còn cờ mặc định TẮT), cộng 6 ⚠️ trong đó 4 cái là lỗi thật: `Boolean(s.stopHookActive)` bị chuỗi `"false"` làm tắt cổng âm thầm; biểu thức chính quy `TEST-RESULT` khớp giữa dòng nên câu hứa "I will add a TEST-RESULT: line later" vượt được cổng; `matchesAgentName` ném lỗi với tham số thứ hai là `null` dù phần đầu file khai "never throws"; và chỉ thị viết cho model bị dùng lại ở nhánh CHẶN của `context-guard`, nơi `exit 2` khiến không có lượt model nào để thực thi nó. Tất cả đã sửa, và lượt sửa còn tìm thêm một chỗ: `implementer-verify-gate.mjs` tự làm `Boolean(...)` TRƯỚC khi gọi hàm thuần, nên sửa hàm thuần một mình là chưa đủ. Bài học: vòng review này chỉ bắt được vì nó CHẠY THẬT rồi so với bản `HEAD`, không phải vì đọc code kỹ hơn.
>
> **Bốn điều kiện còn thiếu để 038, 039, 040, 041 lên `done`, mỗi cái gắn với một task cụ thể:** (a) **038** — chụp payload `SubagentStop` THẬT để biết `stop_hook_active` có tồn tại hay không; hiện có HAI đường hỏng đã ghi vào chú thích: trường vắng thì cổng chặn vô hạn, trường là `true` ngay lần đầu thì cổng không bao giờ chặn dù đã bật cờ. (b) **039** — chụp `agent_type` THẬT của payload `SubagentStart`, để xác nhận nghi vấn `agent-rules-inject` từng chết im lặng (đã vá phòng ngừa, nhưng chưa xác nhận). (c) **040** — nạp lại plugin rồi xem lời nhắc compact có đúng ngôn ngữ người dùng và đúng việc phiên đang làm. (d) **041** — quan sát trọn gói BỐN công tắc rồi HOÀN NGUYÊN `hooks.json`. Ngoài ra, người dùng nên chạy `/code-review` chính thức bằng tay nếu môi trường của họ có, vì vòng correctness ở trên là bản thay thế bằng `ccf-spec-checker` chứ không phải chính lệnh đó. Chưa có commit nào (người dùng chưa yêu cầu).
> **Bốn công tắc, chưa cờ nào từng được thấy chạy thật:** `--hard-block`, `--auto-verify`, `--enforce-tests`, `--dual-channel-stop`. Tất cả đang TẮT trong `hooks.json`. Sau khi quan sát PHẢI hoàn nguyên `hooks.json`, vì đó là file phát hành. Đây là món nợ tích luỹ từ `028a`/`034a`, nay thêm một cờ nữa.
> **Điểm phải cẩn thận về `agent_type`:** số liệu 39/39 đo được ở `subagent_type` phía LỆNH GỌI, còn `agent_type` trong payload hook `SubagentStart` là chỗ KHÁC, harness có thể chuẩn hoá. Chưa được kết luận vượt bằng chứng. `io.test.mjs` mang chú thích "ASSUMPTION FLAGGED AS UNVERIFIED" ngay tại ca liên quan.
> **Artifact-count impact:** NONE — giữ **6 cmd / 6 agent / 9 hook / 1 skill**. `agent-match.mjs` và `io.test.mjs` là THƯ VIỆN, không phải hook.
> **Cách iteration này chạy, ghi thật:** KHÔNG qua `/ccf:plan` đầy đủ. `ExitPlanMode` bị người dùng từ chối ba lần, sau đó người dùng đặt mục tiêu `/ccf:cook`, nên 6 task được thi hành bằng phiên `ccf-implementer` được chỉ thị trực tiếp (mỗi phiên kèm `run_in_background: false`, model mặc định `sonnet` vì chế độ không-hỏi chặn công cụ hỏi — đã nói rõ ra thay vì im lặng, đúng điều task 036 bắt buộc). Suốt phần lớn phiên, quyền ghi vùng `.claude/` bị chặn nên phần đồng bộ spec được làm SAU cùng, khi người dùng mở quyền.
> **Hai chỗ vượt hạn mức, phát hiện lúc `/ccf:updatespec` chạy bước 3:** `.claude/rules/hooks.md` **68 dòng** và `components.md` **55 dòng**, quá mức "mỗi file rule dưới 50 dòng". Đây là drift tích luỹ từ trước, không do iteration này gây ra, nhưng iteration này làm nó tệ thêm. KHÔNG tự tách trong lượt này vì tách sai sẽ làm hỏng chuỗi `@import`; đề xuất một task riêng cắt `hooks.md` theo sự kiện (`Stop`/`SubagentStart`/`SubagentStop`/`UserPromptSubmit`/`PreToolUse`) rồi `@import` lại. Ghi chú thật: cả hai file có ít dòng nhưng mỗi dòng là một đoạn văn rất dài, nên số dòng đang che mất độ dài thực.
> **Hoãn:** **task 042** hàm kiểm tra "review đã xong" (nhánh MỘT của 039 cho phép, dùng cách LOẠI mẫu ack, predicate phải HẸP kèm nhánh cho qua khi transcript không có `tool_result` nào để không bao giờ treo phiên); gộp khối quy tắc văn phong đang sao y 4 bản vào MỘT nguồn; hook `PreToolUse` cưỡng chế `run_in_background: false` thay cho 12 lời nhắc bằng prompt; và các việc đã rà nhưng chưa làm (nguồn `fork`/`resume` cho `session-start`; dạng `args` mảng và field `if` của hook; `effort` cho lệnh và skill; mục "Current plan" của `CLAUDE.md` quá dài; `/verify`, `/ultrareview`; field `modified` cho ghi chú nhớ; 7 sự kiện hook mới và quyết định `defer` của `PreToolUse`).
