# Implementation Plan — CCF (multi-iteration backlog; lead iteration at top)

> **Execution rule: STRICTLY SEQUENTIAL.** Do exactly one task at a time, in order.
> These tasks are slices sequenced for serial execution (thinnest → richest). Each `Depends on` = the prior task in the queue (serial law), unless a real data dependency is noted.
> Do not start task N+1 until task N's **gate is GREEN** (implemented + tested + checked).
> The `in-progress`/`in-review` status is read by the session-start hook to re-load context after compact — keep status up to date.

## Origin — bestpractice-audit → advisor+goal docs → SubagentStop verify-gate (tasks 033–034 + 034a) — DONE (v0.6.0; 034a closed by user acceptance, UN-OBSERVED)
Approved plan `~/.claude/plans/hi-n-t-i-t-nh-n-ng-compiled-blanket.md` (reviewed 2× by `ccf-spec-checker`: round 1 — 2 ❌ [`last_assistant_message` có thể thiếu evidence vì implementer step 8 = update PLAN.md; thiếu bộ-ngắt-loop xác định khi `stop_hook_active` vắng] + 4 ⚠️ + premortem 2×H/3×M → all folded; round 2 — 2 ❌ RESOLVED + 3 ⚠️ [count-drift chưa kín ở root README dùng BẢNG không numeral; phantom `AGENTS.md`; detector false-block task prose-only] → all folded → READY). User hỏi `/ccf-plan` có đúng repo cộng đồng `shanraisshan/claude-code-best-practice` + tính năng mới áp dụng được. **Grounded** (agent `claude-code-guide` + docs `code.claude.com`): `/ccf-plan` đạt 6/7 tiêu chí planning, thiếu duy nhất cross-model review → Task 033. **Bác bỏ đã ground:** `<important if>` KHÔNG tồn tại (CCF đã đúng `paths:`); `/ultraplan` không tồn tại; worktrees/agent-teams song song mâu thuẫn strictly-sequential; **grill-me `context: fork` BẤT KHẢ THI** (AskUserQuestion không khả dụng trong subagent — [sub-agents.md L318-320]) → user bỏ B. **User chốt:** A (`/advisor` cross-model) + D (`/goal`) → Task 033 doc; C (`SubagentStop` ép implementer test) → Task 034 opt-in phòng-thủ + 034a hanging. Test discipline OFF (prose + 1 hook lib vẫn `node --test`). Memory: [[ground-claude-facts-primary-not-blog]], [[ccf-enforce-with-hook-not-prompt]], [[ccf-plugin-runs-from-cache-not-repo]], [[user-delegates-pick-best-practice-not-complex]].

## Task backlog — bestpractice-audit + advisor/goal + SubagentStop-gate (in execution order)
| # | Slice | Layers | Gate (tests green) | Depends on | Status |
|---|-------|--------|--------------------|-----------|--------|
| 033 | Khuyến nghị lệnh official tùy chọn: `/advisor` cross-model (ccf-plan step 6 + ccf-check + tooling.md) + `/goal` (ccf-cook §8 + tooling.md) — KHÔNG hạ cấp gate bắt buộc | 3 cmd + 1 rule (+ README điều kiện) | grep `/advisor`+`/goal` present + `validate` + `tsc` baseline + `node --test` baseline + cross-read (gate hiện có nguyên) | — | done |
| 034 | Hook `SubagentStop` OPT-IN `implementer-verify-gate.mjs` (matcher `ccf-implementer`, `--enforce-tests` default off) + pure `lib/implementer-verify.mjs` + test + `io.mjs#blockSubagentStop` + ghim Return-format `ccf-implementer` (`TEST-RESULT:`/`n/a`) + count 8→9 | 1 hook + 1 lib + test + io.mjs + agent + hooks.json + hooks.md + count-surfaces | `node --test` (nhóm mới + baseline) + `tsc` + stdin-smoke ≥4 + `validate` (no Duplicate) + grep hai-lớp (numeral `9 hook` + row `implementer-verify-gate` ở 3 README) | 033 | done |
| 034a | Live post-reload verify (quan sát SubagentStop payload thật: `transcript_path`?/`stop_hook_active`? + loop-termination; chụp fixture) | live | user reload + bật `--enforce-tests` → quan sát THẬT; **NOT waivable-without-observation** | 034 | done (by user acceptance — UN-OBSERVED) |

> Status: `todo` / `in-progress` / `in-review` / `done` / `blocked`. Lifecycle: `todo → in-progress → in-review → done` — `ccf-implementer` reaches `in-review`; only `/ccf:ccf-updatespec` writes `done` after `/ccf:ccf-check` + `/code-review` pass. `034a` là live-verify treo có **HARD close-gate** (mô phỏng 022a/024a/028a/029a/030a/032a): `--enforce-tests` mặc-định-TẮT tới khi 034a quan sát payload thật → đóng-không-quan-sát không gây loop thật. KHÔNG chặn task khác. **034a nay đóng bằng user-acceptance (UN-OBSERVED)** — toggle giữ default-OFF; phải chụp fixture payload thật trước khi bật, xem Closed note.
> Per-task detail in `task-033-*.md` / `task-034-*.md` / `task-034a-*.md`. 033/034 chạy FRESH session via `ccf-implementer` (alias `sonnet`); 034a user-driven.
> **Independence:** disjoint files khỏi các iteration mở khác TRỪ count-surface (CLAUDE.md/READMEs/hooks.json desc) — 034 làm count-edit 8→9 CUỐI cùng rồi re-grep. 033 đụng `ccf-plan.md`/`ccf-check.md`/`ccf-cook.md`/`tooling.md` (prose thuần).
> **Artifact-count impact:** 033 = NONE (prose); 034 = **+1 hook → 6 cmd / 6 agent / 9 hook / 1 skill**. Sync numeral SỐNG `CLAUDE.md:17` + `plugins/ccf/README.md` + THÊM row bảng hook 3 root README + `hooks.json` description. `tsconfig` glob `hooks/**/*.mjs` đã phủ.

## Closed — bestpractice-audit + advisor/goal + SubagentStop-gate (033 + 034 + 034a) — v0.6.0
Chạy trọn qua `/ccf-cook` trong session này: implement tuần tự 033→034 (fresh `ccf-implementer`, alias `sonnet`) → batch-verify. **`ccf-spec-checker`** (fresh-context) + **`/code-review`** (high, 8-angle line-by-line) chạy song song: `/code-review` bắt **1 ❌ CORRECTNESS** [`implementer-verify-gate.mjs` đọc `input.transcript_path` (KHÔNG document cho SubagentStop) mà BỎ QUA field document `last_assistant_message` → trên harness thật `lastMessage=""` → block loop vô hạn khi bật `--enforce-tests`] + `ccf-spec-checker` **1 ❌ count-drift** [3 root README dòng 124 vẫn "8 hooks" mâu thuẫn bảng 9-row] + 1 ⚠️ helper-list. **Both fixed** (fresh `ccf-implementer`): hook nay đọc `last_assistant_message` TRƯỚC qua pure `resolveLastMessage`, fallback `transcript_path` chỉ khi vắng; README 8→9 + helper-list. Re-verify `ccf-spec-checker` **CLEAN**. `/simplify` no-op (diff nhỏ đã sạch, không reuse-miss — `extractLastAssistantText` là mới thật). Re-gate: `node --test` **162/162**, `tsc` exit 0, `claude plugin validate` passed, grep `8 hook`=0 sống. Shipped: 033 `/advisor`+`/goal` khuyến nghị tùy chọn (không hạ cấp gate); 034 hook `implementer-verify-gate` opt-in default-OFF + `io.mjs#blockSubagentStop` + Return-format `ccf-implementer` ghim `TEST-RESULT:`. Count **6/6/9/1**. **034a vẫn `todo`** (HARD gate: cần reload + bật `--enforce-tests` + quan sát payload SubagentStop THẬT (`transcript_path`?/`stop_hook_active`?) + loop-termination; nếu `stop_hook_active` vắng → toggle vô-hiệu-vĩnh-viễn; chụp fixture điền test 034; NOT waivable-without-observation). **Released v0.6.0** (commit `7c62f4c` + pushed to main per user). **`034a` marked `done` by EXPLICIT user acceptance, NOT by an executed live-verify** (mirrors the 024a precedent, PLAN.md history): the user chose to close without reloading + observing the real `SubagentStop` payload. The live behavior therefore remains UN-OBSERVED — it is NOT known whether the real payload carries `transcript_path` or `stop_hook_active`. **Correctness safety rests on the mechanism, not observation:** the hook is `--enforce-tests` **default-OFF**, so an unobserved close causes NO real loop (the toggle is off). If the toggle is ever enabled: FIRST capture a real `SubagentStop` stdin payload, fill the `implementer-verify.test.mjs` fixture placeholder, and if `stop_hook_active` is absent keep the toggle permanently disabled — re-open this iteration with the captured fixture if the live shape differs from the assumption. Memory: [[askuserquestion-not-in-subagents]], [[subagentstop-shape-unconfirmed-read-last-assistant-message]], [[ccf-plugin-runs-from-cache-not-repo]].

---

## Origin — retire-ccf-test + rename-cook (post-hoc cleanup) — DONE (awaiting commit)
User cleanup applied directly (không qua ccf-plan — thay đổi cấu trúc gọn, do user chỉ định): (1) **XÓA** command `/ccf:ccf-test` — năng lực thiết kế ma trận EP/BVA/decision-table đã dư thừa vì `ccf-implementer` vốn viết test failing-first (gồm ma trận khi discipline ON) ngay trong lúc implement; mọi tham chiếu `/ccf:ccf-test` ở `ccf-cook`/`ccf-check`/`ccf-plan`/`ccf-implementer`/`verify-chain.mjs`(+test)/`auto-verify.mjs`/rules/3 README/CLAUDE.md/AGENTS.md đổi thành "chạy lệnh test của dự án để xác nhận ma trận pass" nơi discipline ON. (2) **ĐỔI TÊN** `/cook` → `/ccf:ccf-cook` (`commands/cook.md` → `commands/ccf-cook.md`) theo quy ước `ccf-*`. `verify-chain.mjs#buildVerifyReason` vẫn gate bước test-suite theo `readDisciplineOn`, chỉ đổi text bước (unit test đổi assert theo text mới). Đã xóa `4rum.md` (bản nháp forum, theo yêu cầu user). Gate xanh: `node --test` **142/142**, `tsc` exit 0, `claude plugin validate` passed. **Count mới: 6 cmd / 6 agent / 8 hook / 1 skill.**

## Origin — cook-orchestrator + nested-spawn-guard iteration (tasks 031–032 + 032a) — OPEN (LEAD)
Approved plan `hi-n-t-i-c-v-n-zesty-pancake.md` (reviewed 2× by `ccf-spec-checker` — round 1: 3 ❌ + 4 ⚠️ + premortem 2×H/2×M → all folded; round 2: READY, 0 ❌, 1 ⚠️ gate-scope + 3 residual-low → all folded). User: (1) subagent tự spawn agent con → chặn; (2) gọi implementer bằng Sonnet 5; (3) lệnh `/cook` mới sau `/ccf-plan` chạy toàn bộ slice tuần tự bằng implementer (Sonnet 5) rồi verify `/ccf-check`+`/code-review` song song → `/simplify` → `/ccf-updatespec`; (4) verify với repo shanraisshan/claude-code-best-practice + docs Anthropic. **Grounding lật spec sai:** Context7 `/websites/code_claude` `/sub-agents`+`/agent-sdk/subagents` — subagent CÓ nested-spawn (depth 5), tool `Agent`(alias `Task`) kế thừa; câu "spawn tools never reach a subagent → no nested spawn" ở `architecture.md`/`components.md` SAI → chặn xác định bằng `disallowedTools: Agent, Task`. `model: sonnet` alias đã resolve Sonnet 5 (`/model-config`) → không đụng trục model. `/cook` KHÔNG dựa SlashCommand (không phải harness nào cũng expose — [[slashcommand-tool-not-always-exposed]]): ưu tiên Task (implementer + ccf-spec-checker) + Skill (`/code-review`/`/simplify`), fallback nhắc tay. `/simplify` GHI file (fan-out cố định 4 agent) → tách chạy sau 2 read-only tránh race; re-gate deterministic SAU simplify TRƯỚC updatespec (fix "done sớm"). **Quyết định người dùng:** slice TUẦN TỰ (giữ luật CCF), chỉ verify song song. Test discipline OFF (thuần frontmatter + markdown command). Memory: [[slashcommand-tool-not-always-exposed]], [[subagent-inherits-project-mcp-via-disallowedtools]], [[ground-claude-facts-primary-not-blog]], [[ccf-plugin-runs-from-cache-not-repo]], [[user-delegates-pick-best-practice-not-complex]].

## Task backlog — cook-orchestrator + nested-spawn-guard (in execution order)
| # | Slice | Layers | Gate (tests green) | Depends on | Status |
|---|-------|--------|--------------------|-----------|--------|
| 031 | Nested-spawn guard: `Agent, Task` vào `disallowedTools` cả 6 agent (implementer OMIT→`disallowedTools: Agent, Task`; 5 read-only nối vào) + prose backup body + sửa spec-sai "no nested spawn" ở `architecture.md`/`components.md` + doc sync | 6 agent + 2 rule | grep frontmatter 6/6 có `Agent, Task` + `claude plugin validate` + `node --test` 134/134 baseline + `tsc` exit 0 + grep không còn "spawn tools never reach" | — | in-review |
| 032 | Lệnh `/ccf-cook` (`commands/ccf-cook.md`: tuần tự → batch-verify 2 read-only song song → re-gate → simplify → updatespec; loại-trừ `auto-verify.mjs`) + wire `ccf-plan` step 8 + entry `tooling.md` + count sync (net **6 command** sau khi retire-ccf-test) | 1 cmd mới + 2 cmd/rule sửa + 4 README + AGENTS.md | `validate` nhận `/ccf-cook` + grep `6 command` (doc sống) + đọc lại `ccf-cook.md` + `tsc` + `node --test` | 031 | in-review |
| 032a | Live post-reload verify (user reload → `/ccf-cook` thật trên backlog nhỏ → implementer tuần tự + Sonnet 5 + no-nest-spawn; batch-verify chạy/fallback; ghi cơ chế invocation thật) | live | user chạy thật + quan sát; **NOT waivable-without-observation** | 032 | todo |

> Status: `todo` / `in-progress` / `in-review` / `done` / `blocked`. Lifecycle: `todo → in-progress → in-review → done` — `ccf-implementer` reaches `in-review`; only `/ccf:ccf-updatespec` writes `done` after `/ccf:ccf-check` + `/code-review` pass. `032a` là live-verify treo có **HARD close-gate** (mô phỏng 022a/024a/028a/029a/030a) — không đóng iteration khi 032a mở, nhưng KHÔNG chặn task khác.
> Per-task detail in `task-031-*.md` / `task-032-*.md` / `task-032a-*.md`. 031/032 chạy FRESH session via `ccf-implementer`; 032a user-driven.
> **Independence:** 031 đụng agent frontmatter (chung vùng 022-023 in-review + 029 done-uncommitted) → xử lý "mở rộng denylist, lấy baseline sau-022-023, re-base nếu revert"; 032 gần như file mới (`cook.md`) trừ count-sync trên CLAUDE.md/README/AGENTS.md/4rum.md (chung mọi iteration + phối hợp 030) → count-edit CUỐI rồi re-grep. Không spawn `ccf-implementer` song song với việc khác.
> **Artifact-count impact:** sau cleanup retire-ccf-test + rename-cook, count net = **6 cmd / 6 agent / 8 hook / 1 skill** (032 thêm `/ccf-cook` nhưng `/ccf-test` bị xóa → net không tăng). Đồng bộ tại CLAUDE.md numeral, `architecture.md:36`, 3 root README, `plugins/ccf/README.md`, `AGENTS.md:14`.

## Closed (partial) — 031 + 032 `in-review`; iteration OPEN until 032a
Cả 031 + 032 implement xong (fresh `ccf-implementer`, alias `sonnet`) + **batch-verify** (dogfood `/cook` flow thủ công trong session này): `ccf-spec-checker` (fresh-context) **1 ❌ medium** [`cook.md` luồng tự-động thiếu `/ccf:ccf-test` có điều kiện khi discipline ON — đã fix: thêm nhánh conditional §3 khớp `buildVerifyReason`] + **3 ⚠️ drift** [`tooling.md:34` tool-policy cũ "writer omits tools"; `CLAUDE.md` status 031 lệch in-progress↔in-review; `CLAUDE.md` đoạn 022-023 còn "no nested spawn" mâu thuẫn 031] — **all fixed**. `/code-review` hợp nhất vào review (diff thuần prose, không code surface). `/simplify` bỏ qua có chủ đích (no code surface → no-op). Re-gate xanh: `claude plugin validate` passed, `tsc` exit 0, `node --test` **142/142**, grep `6 command`=0 hit trên doc sống. Shipped: 6 agent `disallowedTools: ... Agent, Task` + leaf-body + spec-fix "nested spawn CÓ, chặn bằng denylist"; `commands/cook.md` (8 bước + conditional ccf-test); wire `ccf-plan` step 8 + `tooling.md`; count 6→7. **KHÔNG mark `done`** — hành vi thật (sequential-spawn + Sonnet 5 + no-nest + Skill-tool invocation) chưa quan sát được (plugin chạy từ cache → cần reload = gate `032a`); tránh lỗi "done-too-early". NOT committed (awaiting user). Memory: [[subagent-can-nest-spawn-block-via-disallowedtools]], [[slashcommand-tool-not-always-exposed]], [[ccf-plugin-runs-from-cache-not-repo]].

---

## Origin — kiro-cli-port iteration (task 030; 030a hanging; 033+ not yet detailed — 031/032 taken by cook iteration) — OPEN
Approved plan `t-i-mu-n-l-m-cho-clever-cook.md`. User: muốn CCF dùng được ĐỒNG THỜI trên cả Claude Code lẫn **Kiro CLI** (AWS terminal-agentic tool, kế thừa Amazon Q Developer CLI) — "vừa dùng được kiro, vừa dùng ở claude". Grounded kiro.dev (2026-07-03, point-in-time — re-ground từng field trước khi code): Kiro dùng `.kiro/` (steering `.kiro/steering/*.md` + **`AGENTS.md` always-included ở workspace-root**, skills `.kiro/skills/<n>/SKILL.md` gọi `/name` — cùng chuẩn Agent Skills với Claude, custom agents `.kiro/agents/*.json` dùng ALLOWLIST `tools` — KHÔNG có `disallowedTools`, hooks trong agent-JSON/`.kiro/hooks/*.json` (events agentSpawn/userPromptSubmit/preToolUse/postToolUse/stop; output KHÁC Claude: exit0→STDOUT thành context, exit2 chỉ preToolUse, Stop→JSON `decision:block` GIỐNG Claude), MCP `.kiro/settings/mcp.json`). KHÔNG có marketplace/plugin.

**K0 (quyết định phạm vi) resolved inline bởi user, sau 2 lần away:** (1) **Hướng = DUAL** — CCF chạy trên CẢ Claude Code lẫn Kiro CLI, không thay thế cái nào; (2) **`AGENTS.md` ở root = MỘT file tool-agnostic dùng chung** — sửa lại bản Codex-draft hiện có (untracked, tham chiếu `.Codex/rules/` KHÔNG tồn tại — một thử nghiệm port CCF→Codex trước đó) thành nội dung trung tính không nhắc tên tool cụ thể, mirror `CLAUDE.md`; đúng tinh thần user: "kiro cần agents.md thì tạo thêm agents.md"; (3) **WIP 6 iteration treo** (017–019/020–021/022–023/025a/028a/029a) — theo TIỀN LỆ THẬT của chính PLAN.md này (nhiều iteration mở song song có ghi chú "Independence" khi file-set rời nhau, vd 024 vs 022/023, 027/028 vs 017–023/025a) → epic Kiro được phép mở SONG SONG vì CÁCH LY HOÀN TOÀN (cây MỚI `plugins/ccf/kiro/` + sửa root `AGENTS.md`, KHÔNG đụng bất kỳ `.mjs`/`agents/*.md`/`commands/*.md`/`hooks.json` nào), miễn KHÔNG spawn `ccf-implementer` song song với việc khác (luật "never run two agents in parallel" vẫn giữ nguyên). Reviewed 2× by `ccf-spec-checker` plan-review (round 1: 3 ❌ [AGENTS.md-root conflict với bản Codex draft; K1a/030a không verify được always-include khi chưa có cơ chế cài; WIP-drift khỏi STRICTLY-SEQUENTIAL] + nhiều ⚠️ + premortem 3×H/1×M → all folded; round 2: READY, 0 ❌, 3 residual ⚠️ nhẹ không-blocking). Test discipline OFF (chưa có matrix block trong `testing.md`). **Right-size**: chỉ 030+030a được chi tiết hoá; 031+ (port skill/agent/hook còn lại) hoãn tới khi 030a cho bài học thật về Kiro. Memory: [[kiro-cli-port-concept-map]], [[root-agents-md-is-codex-port]], [[ground-claude-facts-primary-not-blog]], [[ccf-plugin-runs-from-cache-not-repo]].

## Task backlog — kiro-cli-port (in execution order)
| # | Slice | Layers | Gate (tests green) | Depends on | Status |
|---|-------|--------|--------------------|-----------|--------|
| 030 | Bộ khung Kiro + lõi chuẩn-chung: sửa root `AGENTS.md` thành tool-agnostic (mirror CLAUDE.md, bỏ ref `.Codex/`) + MỚI `plugins/ccf/kiro/.kiro/steering/{product,tech,structure}.md` + `.kiro/settings/mcp.json`(context7+microsoft-learn) + skill tự-chứa `.kiro/skills/ccf-updatespec/SKILL.md`(reuse `commands/ccf-updatespec.md`) + `.kiro/skills/grill-me/SKILL.md` + count reframe per-platform ở `architecture.md`/CLAUDE.md + repo-layout doc-sync | steering+skill+MCP+doc (0 hook code) | JSON.parse mcp.json; frontmatter skill hợp lệ (`name` kebab≤64, `description`≤1024); AGENTS.md root <200 dòng + grep invariant lõi; re-ground từng field Kiro qua kiro.dev + lưu 1 `.kiro/` mẫu làm fixture; `claude plugin validate plugins/ccf` vẫn pass; grep-consistency per-platform | — | todo |
| 030a | Live post-cài verify (SONG SONG, gate `done`/release — KHÔNG gate task 031+): user copy artifact vào 1 scratch dir có AGENTS.md ở root → chạy Kiro CLI thật → `/ccf-updatespec` xuất hiện; AGENTS.md+steering nạp; MCP `context7`/`microsoft-learn` `Connected` | live | user chạy Kiro thật + quan sát; NOT waivable-without-observation | 030 | todo |

> Status: `todo` / `in-progress` / `in-review` / `done` / `blocked`. Lifecycle: `todo → in-progress → in-review → done` — `ccf-implementer` reaches `in-review`; only `/ccf:ccf-updatespec` writes `done` after `/ccf:ccf-check` + `/code-review` pass. `030a` là live-verify treo có **HARD close-gate** (mô phỏng 022a/024a/025a/028a/029a) — NOT waivable-without-observation, nhưng KHÔNG chặn việc bắt đầu task 031 (031 dựa gate STRUCTURAL của 030) — tránh bài học 024a "cả chuỗi chặn bởi 1 live-verify cần môi trường ngoài".
> Per-task detail in `task-030-*.md` / `task-030a-*.md`.
> **Independence:** cách ly HOÀN TOÀN khỏi 6 iteration đang treo (017–019/020–021/022–023/025a/028a/029a) — file-set rời nhau tuyệt đối (cây MỚI `plugins/ccf/kiro/` + root `AGENTS.md`, không đụng `.mjs`/`agents/*.md`/`commands/*.md`/`hooks.json`); không spawn `ccf-implementer` song song với việc khác.
> **Artifact-count impact:** Claude-side KHÔNG đổi (vẫn 6 cmd / 6 agent / 8 hook / 1 skill). Kiro-side MỚI, đang triển khai — 030 thêm 2 skill Kiro (`ccf-updatespec`, `grill-me`), 0 agent, 0 hook Kiro (hoãn tới 031+). Ghi rõ "Kiro-side: 2/0/0/2 (đang triển khai)" tại điểm count-reframe.

---

## Origin — agent-effort iteration (tasks 029 + 029a) — OPEN
Approved plan `reflective-napping-shell.md`. User: lúc spawn agent khi implement, UI không hiện nhãn model "(sonnet)" → nghi "agent không tự chọn model". Grounded (Context7 `/websites/code_claude` `/sub-agents` + `/costs` + `/model-config` + changelog): `model:` frontmatter ĐÃ là cơ chế ép model chính thức và CCF đã khai báo đúng cho cả 6 agent (omit→`inherit`); việc không hiện nhãn là HIỂN THỊ, không phải chọn sai model → không sửa trục model. Cơ chế MỚI thật sự = field **`effort`** (changelog **2.1.78**: plugin-shipped agents now support `effort`/`maxTurns`/`disallowedTools`; user chạy 2.1.178). `effort` = reasoning DEPTH ⟂ `model` (không thay model); `low|medium|high|xhigh|max`; omit→inherit session; unsupported→fallback xuống mức hỗ trợ cao nhất ≤ nó (`xhigh` trên Sonnet 4.6→`high`). User chốt: thêm `effort` cho cả 6 agent theo cost/difficulty — analyzer=`low` (scan 5-parallel rẻ), implementer/researcher/writer=`medium` (việc đã spec rõ), spec-checker/debugger=`high` (high-stakes; `high`=default Opus 4.8, `max` diminishing returns). **Key de-risk:** KHÔNG hook nào parse frontmatter agent (`output-style.mjs`/`agent-rules-inject.mjs`/`review-trace.mjs` không đọc field agent; hook đọc `model` lấy từ transcript `message.model`) → thay đổi frontmatter+prose THUẦN, không động logic, count artifact GIỮ NGUYÊN 6/6/8/1, không test/tsc mới (chạy baseline chống hồi quy). Test discipline OFF. Memory: [[subagent-model-selection]], [[ground-claude-facts-primary-not-blog]], [[ccf-plugin-runs-from-cache-not-repo]].

## Task backlog — agent-effort (in execution order)
| # | Slice | Layers | Gate (tests green) | Depends on | Status |
|---|-------|--------|--------------------|-----------|--------|
| 029 | Thêm `effort` vào frontmatter 6 agent (analyzer=low; implementer/researcher/writer=medium; checker/debugger=high) + prose sync (`components.md:14`+đoạn pick-model:22; `architecture.md:9`) | 6 agents + 2 rules | grep frontmatter (mỗi agent `model`+`effort` khớp bảng) + `validate` + `node --test` 134/134 baseline + `tsc` exit 0 + grep-consistency prose | — | done |
| 029a | Live post-reload verify (user reload plugin → spawn agent thật → `effort` có hiệu lực/nhãn hiển thị đúng) | live spawn | user reload + spawn thật → quan sát effort áp dụng | 029 | todo |

> Status: `todo` / `in-progress` / `in-review` / `done` / `blocked`. Lifecycle: `todo → in-progress → in-review → done` — `ccf-implementer` reaches `in-review`; only `/ccf:ccf-updatespec` writes `done` after `/ccf:ccf-check` + `/code-review` pass. `029a` is a DELIBERATE hanging verify with a **HARD close-gate** (modeled on 028a/025a/024a): plugin active runs from cache → `effort` thật chỉ thấy sau reload; not-`done` until OBSERVED, NOT waivable by acceptance-without-observation.
> **Artifact-count impact:** NONE — frontmatter field add on 6 agents + prose; no command/agent/hook/skill add/remove. Counts stay **6 cmd / 6 agent / 8 hook / 1 skill**. `hooks.json`/`tsconfig.json`/`plugin.json` untouched (version bumped to 0.4.5 at release).

## Closed (partial) — 029 `done`; iteration OPEN until 029a
Task 029 `done` + `/ccf:ccf-check` (ccf-spec-checker, opus, fresh-context) **READY — 0 ❌, 0 drift** + `/code-review` high (**1 doc-drift finding fixed**: CLAUDE.md "Current plan" còn nêu iteration cũ là lead → cập nhật agent-effort lên lead, đúng [[claude-md-current-plan-stale]]; mọi angle khác clean). Gates green: `node --test` **134/134**, `tsc` exit 0, `claude plugin validate` passed, grep frontmatter 6/6 khớp matrix, CLAUDE.md 38 dòng (<200). Shipped: `effort` frontmatter trên cả 6 agent (analyzer=low; implementer/researcher/writer=medium; checker/debugger=high) + prose `components.md`(field-list:14 + pick-effort bullet:22 + "re-eval effort+model together") + `architecture.md:9`; CLAUDE.md "Current plan" sync. **`/ccf:ccf-check` cải thiện đã fold:** thêm dòng "re-evaluate effort+model together" (L-premortem). Counts **6/6/8/1**. Released v0.4.5. **`029a` vẫn `todo`** (HARD gate: cần user reload plugin + spawn agent thật + quan sát effort HIỆU LỰC trên model thật (Opus 4.8/2.1.178), ghi mức observed vào close note; NOT waivable-without-observation). Memory: [[subagent-model-selection]], [[ccf-plugin-runs-from-cache-not-repo]].

---

## Origin — right-size-slices + auto-verify-chain iteration (tasks 027–028 + 028a) — OPEN
Approved plan `hi-n-t-i-v-n-humming-fountain.md`. User: (1) slice chia quá nhỏ → "chỉ chia nhỏ khi cần"; (2) muốn `/ccf-test`+`/ccf-check`+`/code-review` TỰ chạy khi code xong (đã cân nhắc biến ccf-test thành skill tự-trigger → **bác bỏ có căn cứ**: skill auto-trigger không xác định). Grounded (`ccf-best-practice-researcher`, Context7 `/websites/code_claude`): tool `SlashCommand` (main loop) gọi được custom command (`/ccf-check`); built-in `/code-review` + gọi-từ-subagent = **evidence-pending**; hook `Stop`+`decision:"block"`+`reason` = ralph-loop (`plugins/ralph-wiggum`), main loop tiếp tục với reason — XÁC ĐỊNH; Anthropic: auto-run verify/review (không phá huỷ) là OK. **Iteration A (027):** thêm nguyên tắc right-size vào `ccf-plan.md`+`architecture.md` (slice = tăng-trưởng cố kết cỡ-PR; chỉ tách khi có driver: data-dep/gate-độc-lập/cô-lập-rủi-ro/không-vừa-context; doc-sync GỘP mặc định) — vẫn giữ vertical-slice. **Iteration B (028+028a):** hook `Stop` OPT-IN `auto-verify.mjs` (arg `--auto-verify`, mặc-định-TẮT) lái main loop chạy `/ccf-check→/code-review→/ccf-test(nếu discipline)→/ccf-updatespec(chỉ khi sạch)`; trigger = `findActiveTask` thấy task `in-review` + `editedCode` + `!checkAlreadyRan`; loop-guard CHÍNH = `stop_hook_active`, `hasSpecCheckerReview` chỉ là guard xuyên-Stop; `io.mjs#blockStop` mới; pure `verify-chain.mjs` (`shouldDriveVerify`/`readDisciplineOn`/`buildVerifyReason`) + test. Hook BLOCKING `Stop` ĐẦU TIÊN → phải sửa doc "PURELY ADVISORY" cho khớp. 028a = live-verify CỨNG (built-in `/code-review` evidence-pending), không waivable-without-observation, bắt buộc commit transcript fixture. Reviewed 2× by `ccf-spec-checker` (round 1: 2 ❌ [Stop advisory→blocking drift chưa liệt kê doc-sync; loop-guard reframing + thiếu ca smoke `stop_hook_active`] + 3 ⚠️ + premortem → all folded; round 2: READY, 1 ⚠️ cosmetic smoke-count đã sửa). Test discipline OFF (pure lib vẫn `node --test`). **Caveat:** plugin active chạy từ cache → hiệu lực thật cần reload → 028a. Memory: [[ccf-enforce-with-hook-not-prompt]], [[hook-user-visible-channel]], [[heuristic-copy-drift-smoke-reveals]], [[ccf-plugin-runs-from-cache-not-repo]], [[user-delegates-pick-best-practice-not-complex]].

## Task backlog — right-size-slices + auto-verify-chain (in execution order)
| # | Slice | Layers | Gate (tests green) | Depends on | Status |
|---|-------|--------|--------------------|-----------|--------|
| 027 | Right-size principle vào `ccf-plan.md` step 4/5 + `architecture.md` (slice cố kết cỡ-PR; 4 split-drivers; doc-sync gộp mặc định; giữ vertical-slice) | 1 cmd + 1 rule (+ ccf-init/template nếu có nhắc) | grep (right-size + 4 drivers; vertical-slice còn nguyên) + `validate` + `tsc` + cross-read | — | done |
| 028 | `auto-verify.mjs` Stop hook OPT-IN (ralph-loop) + `verify-chain.mjs` (3 pure fn) + test failing-first + `io.mjs#blockStop` + hooks.json Stop#2 + doc Stop-advisory→blocking + FULL count sync 7→8 | 1 hook + 1 lib + test + io.mjs + hooks.json + 2 rules + CLAUDE.md + 4 README | `node --test` (nhóm verify-chain + baseline) + `tsc` + stdin-smoke **5 ca** + `validate` (no Duplicate hooks) + grep `8 hook`+description | 027 | done |
| 028a | Live post-reload verify (real implementer→in-review→Stop→main loop tự chạy `/ccf-check`(+`/code-review` nếu được); chụp+commit transcript) | live session | user reload + bật `--auto-verify` → quan sát THẬT + commit fixture | 028 | todo |

> Status: `todo` / `in-progress` / `in-review` / `done` / `blocked`. Lifecycle: `todo → in-progress → in-review → done` — `ccf-implementer` reaches `in-review`; only `/ccf:ccf-updatespec` writes `done` after `/ccf:ccf-check` + `/code-review` pass. `028a` is a DELIBERATE hanging verify with a **HARD close-gate** (modeled on 025a/024a/022a/017): not-`done` until the REAL post-reload behavior is OBSERVED + the captured transcript is COMMITTED as a fixture — explicitly **NOT waivable by acceptance-without-observation** (the 024a failure mode, PLAN.md:40); `/ccf:ccf-updatespec` must NOT close the iteration while `028a` is open.
> Per-task detail in `task-027-*.md` / `task-028-*.md` / `task-028a-*.md`. Tasks 027/028 run in a FRESH session via `ccf-implementer`; 028a is user-driven.
> **Independence:** 027/028 độc lập file với các iteration mở (017–019/020–021/022–023/025a) TRỪ va chạm vùng-mã THẬT ở `architecture.md` (§Command↔agent boundary, đang bị nhiều iteration in-review chạm) + numeral/list count trên CLAUDE.md/READMEs — 027 rebase `architecture.md`, 028 làm count-edit (7→8) CUỐI cùng rồi re-grep.
> **Artifact-count impact:** 027 = NONE (prose); 028 = **+1 hook → 6 cmd / 6 agent / 8 hook / 1 skill**. Sync at `CLAUDE.md` numeral+`:17`, 3 root READMEs (count+table), `plugins/ccf/README.md` tree, `hooks.json:2` description. tsconfig NOT touched (glob `hooks/**/*.mjs` covers).

## Closed (partial) — 027 + 028 `done`; iteration OPEN until 028a
Tasks 027 + 028 `done` + `/ccf:ccf-check` (ccf-spec-checker fresh-context) **APPROVE 0 ❌** + `/code-review` **0 lỗi correctness**. Gates green: `node --test` **134/134**, `tsc` exit 0, `claude plugin validate` passed (no Duplicate hooks), 5/5 stdin-smoke đúng, **mutation test** (đảo `shouldDriveVerify` → 3 test đỏ → test phân biệt được). Shipped: nguyên tắc right-size trong `ccf-plan.md`+`architecture.md` (vertical-slice/sequential/refactor laws còn nguyên); hook BLOCKING `Stop` đầu tiên `auto-verify.mjs` (opt-in `--auto-verify`, default no-op) + `verify-chain.mjs` (3 pure fn) + `io.mjs#blockStop` + doc Stop "TWO hooks" advisory+blocking; count 7→8 nhất quán. **Live finding ([[slashcommand-tool-not-always-exposed]]):** trong session check này tool `SlashCommand` KHÔNG được expose → built-in `/code-review` không auto-invoke được → nhánh fallback "nhắc người dùng" là path thực tế → giữ 028a trung thực. **028a vẫn `todo`** (HARD gate: cần user reload + bật `--auto-verify` + quan sát thật + commit transcript fixture; KHÔNG waivable-without-observation). Counts **6/6/8/1**. NOT committed (awaiting user). Memory: [[ccf-enforce-with-hook-not-prompt]], [[hook-user-visible-channel]], [[slashcommand-tool-not-always-exposed]].

---

## Origin — explore-lsp-guidance iteration (tasks 025–026 + 025a) — OPEN
Approved plan `t-i-mu-n-c-i-thi-n-playful-curry.md`. User: muốn việc explore source nhanh/thông minh hơn bằng LSP + ripgrep + "công nghệ hot". Phát hiện then chốt: khi user hỏi về source, main loop spawn agent **`Explore` built-in** (KHÔNG phải `ccf-codebase-analyzer` — cái đó chỉ `/ccf-init` gọi). CCF không sở hữu prompt của `Explore` → đòn bẩy xác định duy nhất là **tiêm guidance lúc spawn** qua `SubagentStart` + `additionalContext` (giống `agent-rules-inject`). Fix = hook MỚI `explore-guide-inject.mjs` (matcher `"Explore"`) tiêm directive ngắn, **trung-lập-ngôn-ngữ, LSP-conditional**: ưu tiên LSP (`workspaceSymbol`/`goToDefinition`/`findReferences`/`documentSymbol`/call-hierarchy) khi có language server, dùng `Grep`(ripgrep) cho text, `Glob` cho filename, fallback grep khi không có server ("don't retry"). Grounded Context7 `/websites/code_claude` (`/hooks`: SubagentStart fire cho built-in, `agent_type:"Explore"` canonical, matcher lọc trên `agent_type`, `additionalContext` reaches built-in; `/tools-reference`: tool `LSP` read-only + inactive tới khi có server → directive phải có điều kiện; `/sub-agents`: Explore giữ LSP+Grep). **EXCLUDE ast-grep** sau research Anthropic/GitHub: stack explore chính thống = Glob+Grep+LSP, "add complexity only when it demonstrably improves outcomes", ast-grep không cài-sẵn + trùng LSP + MCP experimental (414★) → bỏ. Reviewed 2× by `ccf-spec-checker` plan-review (round 1: ⚠️ APPROVE — 1 must-fix [PLAN.md baseline note vs 3 iteration mở] + count-sync surface + premortem 1×H/2×M/1×L → all folded; round 2: 1 ❌ [architecture.md không có numeral hook — sửa lý do] → fixed → READY). Test discipline OFF (pure lib vẫn `node --test`). **Caveat:** plugin active chạy từ cache → hiệu lực thật cần reload → tracked ở `025a` (HARD close-gate). Memory: [[ccf-enforce-with-hook-not-prompt]], [[subagent-spawn-tool-named-agent]], [[ccf-plugin-runs-from-cache-not-repo]], [[output-style-not-inherited-by-subagents]], [[user-delegates-pick-best-practice-not-complex]].

## Task backlog — explore-lsp-guidance (in execution order)
| # | Slice | Layers | Gate (tests green) | Depends on | Status |
|---|-------|--------|--------------------|-----------|--------|
| 025 | `explore-guide-inject.mjs` SubagentStart hook (matcher `"Explore"`) + `lib/explore-guide.mjs` `buildExploreGuidance` + test failing-first + hooks.json wiring + matcher-key fixture/evidence-pending | 1 hook + 1 lib + test + hooks.json | `node --test` (nhóm explore-guide + 113 baseline) + `tsc` + stdin-smoke (`agent_type:"Explore"`→additionalContext) + `validate` | — | done |
| 026 | Spec + docs + count sync 6→7 hook (hooks.md entry + CLAUDE.md numeral+prose:17 + architecture.md mention + hooks.json:2 desc + 3 README + plugin README) | 2 rules + CLAUDE.md + 4 READMEs + hooks.json + PLAN.md | grep-consistency một-pass (`7 hook`, `explore-guide-inject`/`SubagentStart`, không `6 hook` sót) + `validate` + `tsc` + `node --test` | 025 | done |
| 025a | Live post-reload verify (real Explore spawn → captured `SubagentStart` `agent_type:"Explore"` + injected `additionalContext` present + Explore gọi LSP/fallback) | live spawn | user reload plugin → hỏi source thật → Explore nhận directive + dùng LSP | 026 | todo |

> Status: `todo` / `in-progress` / `in-review` / `done` / `blocked`. Lifecycle: `todo → in-progress → in-review → done` — `ccf-implementer` reaches `in-review`; only `/ccf:ccf-updatespec` writes `done` after `/ccf:ccf-check` + `/code-review` pass. `025a` is a DELIBERATE hanging verify with a **HARD close-gate** (modeled on 024a/022a/017): it stays not-`done` until the user reloads + the REAL Explore spawn is OBSERVED (captured transcript shows the injected `additionalContext`) — explicitly NOT waivable by acceptance-without-observation (the 024a failure mode); `/ccf:ccf-updatespec` must NOT close the iteration while `025a` is open.
> Per-task detail in `task-025-*.md` / `task-026-*.md` / `task-025a-*.md`. Tasks 025/026 run in a FRESH session via `ccf-implementer`; 025a is user-driven.
> **Independence:** disjoint files from open iterations 017–019/020–021/022–023 EXCEPT the shared hook-count numeral/list on CLAUDE.md/READMEs — 026 rebases onto whatever count those leave and does the count edit last.
> **Artifact-count impact:** **+1 hook → 6 cmd / 6 agent / 7 hook / 1 skill.** Sync at `CLAUDE.md` numeral+`:17` prose list, 3 root READMEs (count+table), `plugins/ccf/README.md` tree. tsconfig NOT touched (glob `hooks/**/*.mjs` already covers).

---

## Origin — context-guard compact-boundary iteration (task 024 + 024a) — DONE (v0.4.1)
Approved plan `hi-n-t-i-ch-c-n-ng-cozy-puzzle.md`. User: chức năng cảnh báo `/compact` (context-guard) chạy ổn, nhưng NGAY SAU khi `/compact` xong, prompt kế vẫn bị cảnh báo "context vẫn vượt ngưỡng" — hook đọc dữ liệu CŨ, không phản ánh việc vừa nén → UX sai dù workflow không hỏng. Root cause (grounded Context7 `/websites/code_claude`): `/compact` KHÔNG tạo transcript mới — chèn `{type:"system",subtype:"compact_boundary",compact_metadata:{trigger,pre_tokens}}` vào CÙNG file (`code.claude.com/docs/en/hooks` PostCompact + `SDKCompactBoundaryMessage`); `readContextUsage` duyệt ngược, prompt đầu sau compact kích `UserPromptSubmit` TRƯỚC khi có dòng assistant mới → đọc dòng assistant phía TRÊN boundary → token trước-nén (lớn). Docs `prompt-caching` xác nhận turn sau compact mang summary ngắn → một khi có assistant mới thì token đã nhỏ. Fix = trong vòng duyệt ngược, gặp `compact_boundary` TRƯỚC dòng assistant-có-usage → `return null` → hook im lặng (đã có sẵn `if(!usage) process.exit(0)` ở `context-guard.mjs:21`); KHÔNG dùng `pre_tokens`. Predicate HẸP `type==="system" && subtype==="compact_boundary"` (KHÔNG fallback `compact_metadata` — quá rộng, mâu thuẫn bài học Bug #4, có thể âm thầm tắt cảnh báo). Reviewed 2× by `ccf-spec-checker` plan-review (round 1: 2 must-fix [predicate quá rộng; predecessor treo sau 022a] + should-do + premortem 3×M → all folded; round 2: READY, 1×L residual đã phủ bởi 024a). Test discipline OFF (pure helper vẫn `node --test`). Memory: [[heuristic-copy-drift-smoke-reveals]], [[subagent-spawn-tool-named-agent]], [[ground-claude-facts-primary-not-blog]].

## Task backlog — context-guard compact-boundary (in execution order)
| # | Slice | Layers | Gate (tests green) | Depends on | Status |
|---|-------|--------|--------------------|-----------|--------|
| 024 | `isCompactBoundary` (predicate hẹp) + 1 nhánh `return null` trong `readContextUsage`; hook KHÔNG đổi; test failing-first + docs sync | 1 lib + test + hooks.md + CLAUDE.md + PLAN.md (+ README có điều kiện) | `node --test` (6 nhóm test mới + 107 baseline) + `tsc` + stdin-smoke 2 ca + `validate` + grep-consistency `compact_boundary` | — | done |
| 024a | Live post-reload verify (real `/compact` trên harness thật → hết cảnh báo ngay sau compact; chụp boundary thật đối chiếu predicate) | live session | user reloads plugin → session thật đẩy context > ngưỡng → `/compact` → prompt kế không còn banner; context lớn lại → banner trở lại | 024 | done |

> Status: `todo` / `in-progress` / `in-review` / `done` / `blocked`. Lifecycle: `todo → in-progress → in-review → done` — `ccf-implementer` reaches `in-review`; only `/ccf:ccf-updatespec` writes `done` after `/ccf:ccf-check` + `/code-review` pass. `024a` is a DELIBERATE hanging verify (modeled on 022a/017): stays not-`done` until the user reloads + the live `/compact` check passes; `/ccf:ccf-updatespec` must NOT close the iteration while `024a` is open.
> Per-task detail in `task-024-*.md` / `task-024a-*.md`. Task 024 runs in a FRESH session via `ccf-implementer`; 024a is user-driven.
> **Independence:** 024 does NOT depend on 022/023 code — disjoint file sets (024 touches `context-usage.mjs`/`context-guard.mjs`/`hooks.md`; 022 touches agent frontmatter/`architecture.md`/`components.md`/`tooling.md`/READMEs). Only co-edit point is `CLAUDE.md` "Current plan" + this `PLAN.md` (narrative, trivial merge). 022a is a human-gated hanging verify → does not block 024.
> **Artifact-count impact:** NONE — 1 new pure helper inside the existing `context-usage.mjs` lib + 1 branch; no new command/agent/hook/skill; `hooks.json` untouched. Counts stay **6 cmd / 6 agent / 6 hook / 1 skill**.

## Closed — context-guard compact-boundary iteration (tasks 024 + 024a) — v0.4.1
Task 024 `done` + `/ccf:ccf-check` APPROVE (0 ❌, 0 drift; gates green: `node --test` **113/113**, `tsc` exit 0, `claude plugin validate` passed, smoke 3-ca: boundary-mới-nhất→im lặng, assistant-nhỏ-sau-boundary→đo lại, assistant-lớn-sau-boundary→cảnh báo trở lại = suppression có phạm vi) + `/code-review` high (0 correctness bug — cả 3 candidate là tradeoff thiết kế đã tài liệu hóa; 2 finding test-quality CONFIRMED đã sửa: Group 4 fixed thành ca discriminating `assistant→user→boundary` (chứng minh: tắt nhánh → 4 test đỏ gồm Group 4), comment Group 3 sửa đúng cơ chế swap). `readContextUsage` nay trả `null` khi gặp `compact_boundary` mới hơn dòng assistant-có-usage cuối → hook im lặng ngay sau `/compact`; predicate HẸP `type:system`+`subtype:compact_boundary` (không `compact_metadata`, không `pre_tokens`). Counts unchanged 6/6/6/1. Released v0.4.1 (commit `8947042`, tag pushed, CI Release success, GitHub Release Latest). Memory: [[test-must-fail-without-logic]], [[compact-boundary-transcript-shape]], [[heuristic-copy-drift-smoke-reveals]]. **`024a` marked `done` by EXPLICIT user acceptance, NOT by an executed live-verify** — user reloaded the plugin (v0.4.1 active, 7 hooks) and chose to close the iteration without running the over-threshold→`/compact`→no-banner check themselves. The live behavior therefore remains UN-OBSERVED in a real session; correctness rests on the unit/smoke/grounding evidence above. If the real harness boundary shape ever differs from `subtype:"compact_boundary"`, the worst case is a safe no-op (warns as before, never crashes) — re-open with a captured-real fixture if observed.

---

## Origin — subagent-tool-inheritance iteration (tasks 022–023) — OPEN
Approved plan `hi-n-t-i-c-c-agent-rustling-wind.md`. User: *"tôi cũng muốn agent có thể dùng skill và mcp hiện có của project"* + *"dự án có bao nhiêu tool/mcp thì cho dùng hết"* → CCF subagents must reach the host project's arbitrary MCP (oracle, chrome-devtools) + project skills. Root cause (grounded Context7 `/websites/code_claude`, `code.claude.com/docs/en/sub-agents` + `/en/tools-reference`): a `tools` **allowlist** BLOCKS every unlisted MCP + the Skill tool; a plugin subagent cannot list unknown-at-authoring-time project MCP and the `mcpServers` field is "Ignored for plugin subagents" → the ONLY mechanism is inheritance (omit `tools` / `disallowedTools`). Fix = uniform: **`ccf-implementer` (writer) omits `tools`** (inherit-all incl. all project MCP + Skill); the **5 read-only agents use `disallowedTools: Write, Edit, NotebookEdit`** (inherit-all-minus-file-writes → keep non-writing mandate + analyzer 5-parallel safety). Safety = file-write denial + runtime permission prompts (each MCP call still prompts) + session-state tools never reach subagents (no nested spawn). Reviewed 2× by `ccf-spec-checker` plan-review (round 1: 3 ❌ [validate is manifest-only not a real gate; researcher loses WebFetch silently; premortem missed substitutability] + 3 ⚠️ [README:61 + components 14/17 rewrite scope; architecture parallel-DB] + premortem → all folded; round 2: all 7 PASS + 2 new doc-consistency holes [stale validate line; verify-row vs status-note] → both fixed → CLEAN). Test discipline OFF (frontmatter/prose `.md`). **Caveat:** active plugin runs from the v0.3.4 cache ([[ccf-plugin-runs-from-cache-not-repo]]) → real effect needs a plugin reload → tracked as the `022a` row. Memory: [[ground-claude-facts-primary-not-blog]], [[ccf-plugin-runs-from-cache-not-repo]], [[output-style-not-inherited-by-subagents]].

## Task backlog — subagent-tool-inheritance (in execution order)
| # | Slice | Layers | Gate (tests green) | Depends on | Status |
|---|-------|--------|--------------------|-----------|--------|
| 022 | 6 subagents inherit project tool/MCP/skill (writer omit `tools`; 5 read-only `disallowedTools: Write,Edit,NotebookEdit` + uniform READ-ONLY body line) | 6 agents | field-validity grounded (docs field-list, NOT validate) + `validate` smoke + **GO/NO-GO dogfood-mechanism** (generic agent calls a project MCP) + grep frontmatter + `tsc` + cross-read | — | in-review |
| 023 | Spec + docs sync (rewrite components 14+17 + 3 README:61 + architecture + tooling + plugin README + CLAUDE.md + this PLAN.md; no count change) | 3 rules + 4 READMEs + CLAUDE.md + PLAN.md | grep-consistency (inheritance everywhere; OLD least-privilege claim gone at components 14/17 + README:61) + `validate` + `tsc` + `node --test` | 022 | in-review |
| 022a | Post-reload live-spawn verify (real `ccf:ccf-implementer`/`ccf-debugger` call a project MCP + skill) | live spawn | user reloads plugin → spawns real CCF subagents → they reach project MCP+skill | 023 | todo |

> Status: `todo` / `in-progress` / `in-review` / `done` / `blocked`. Lifecycle: `todo → in-progress → in-review → done`. `022a` is a DELIBERATE hanging verify (modeled on 017's held live-check): it stays not-`done` until the user reloads + the real spawn passes; `/ccf:ccf-updatespec` must NOT close the iteration while `022a` is open.
> Per-task detail in `task-022-*.md` / `task-023-*.md`. Each task runs in a FRESH session via `ccf-implementer`.
> **Artifact-count impact:** NONE — frontmatter `tools`→`disallowedTools`/omit on 6 agents + body wording; no command/agent/hook/skill add/remove. Counts stay **6 cmd / 6 agent / 6 hook / 1 skill**.

---

## Origin — premortem-reviewer iteration (tasks 020–021) — OPEN
Approved plan `hi-n-t-i-c-c-agent-rustling-wind.md`. User: is there a better plan-review process worldwide? Research (SOTA): CCF's current plan-review (evaluator-optimizer + grounded LLM-judge + deterministic gate + human-in-the-loop) is already best-practice but only checks **structure** (vertical slice / gate / predecessor / drift) — it never asks *"how will this plan FAIL."* The repo's own `PLAN.md` "Closed" history shows recurring misses (count-drift across READMEs 005–009; `/code-review` finding 4 bugs AFTER `done` → birth of `in-review`; deferred live-checks 012–014/017). Fix = add a **premortem / prospective-failure lens** to `ccf-spec-checker`'s plan-review mode, anchored to the project's `PLAN.md` "Closed" + memory (PreFlect: prospective critique must anchor to REAL history). Chosen as a SECTION inside `ccf-spec-checker` (not a new agent / second pass) because the gate `review-trace.mjs:71` keys on `subagent_type` "ccf-spec-checker" → auto-recognized, **0 hook change, count 6/6/6/1, mandatory by construction**. Soft-gate via the existing step-6 loop (H-likelihood unmitigated = ❌, with recorded disposition), NOT a deterministic hook (a hook can't judge premortem quality — `architecture.md` "judgment → prompt"). Reviewed 1× by `ccf-spec-checker` (2 ❌ + 2 ⚠️ + dogfood premortem → all folded: `ccf-init.md` second-invoker added to 020, both README tables to 021, architecture-anchor by-fit, scope-exclusion stated, in-session dogfood verify replaces the deferred one, H-disposition recording added). Grounding: Anthropic "building effective agents" (*add complexity only when it demonstrably improves outcomes*), criteria-decomposition (AIME), PreFlect/prospective-hindsight, LLMs-cannot-self-correct-without-grounding. Test discipline OFF (pure `.md` prose). DEFERRED next iteration (022+, user-requested): subagent access to project skill + project-specific MCP. Memory: [[ground-claude-facts-primary-not-blog]], [[ccf-enforce-with-hook-not-prompt]].

## Task backlog — premortem-reviewer (in execution order)
| # | Slice | Layers | Gate (tests green) | Depends on | Status |
|---|-------|--------|--------------------|-----------|--------|
| 020 | Premortem lens in `ccf-spec-checker` + wire `ccf-plan` step 6 + `ccf-init` A4 gate (request premortem, record H-disposition) | agent + 2 commands | grep wording (premortem/Closed/🔮/disposition) + `validate` + `tsc` + cross-read + **DOGFOOD spawn** (🔮 block anchored to a real "Closed") | — | done |
| 021 | Spec + docs sync (premortem in rules + BOTH README tables + plugin README + CLAUDE.md + PLAN.md; no count change) | 2 rules + 3 READMEs + plugin README + CLAUDE.md + PLAN.md | grep-consistency (premortem across all invokers + both README tables; counts 6/6/6/1) + `validate` + `tsc` + `node --test` | 020 | done |

> Status: `todo` / `in-progress` / `in-review` / `done` / `blocked`. Lifecycle: `todo → in-progress → in-review → done` — `ccf-implementer` reaches `in-review`; only `/ccf:ccf-updatespec` writes `done` after `/ccf:ccf-check` + `/code-review` pass.
> Per-task detail in `task-020-*.md` / `task-021-*.md`. Each task runs in a FRESH session via `ccf-implementer`.
> **Artifact-count impact:** NONE — premortem is a SECTION inside the existing `ccf-spec-checker` (no new command/agent/hook/skill); gate `plan-review-gate.mjs` untouched. Counts stay **6 cmd / 6 agent / 6 hook / 1 skill**.

## Closed — premortem-reviewer iteration (tasks 020–021)
Both tasks `done` + `/ccf:ccf-check` APPROVE (0 ❌, 0 in-scope drift; gates green: `node --test` 107/107, `tsc` exit 0, `claude plugin validate` passed, git-diff scope = 11 in-scope files, scope-excluded `ccf-check.md`/`tooling.md.tmpl` untouched) + `/code-review` (right-sized 2-finder fan-out for a pure-prose diff → 4 real findings, ALL fixed + re-verified): (1) `hooks.md` "expects exactly ONE review" was false (the gate is satisfied by the FIRST review; a loop-until-clean plan may produce several) → corrected; (2) `ccf-spec-checker.md` premortem ❌-vs-🔮 incoherence → an H-unmitigated finding is now "blocking (must-fix), flagged in 🔮 as likelihood H"; (3) `ccf-spec-checker.md` had hardcoded CCF-internal iteration numbers (005–009…) in the SHIPPED agent body → generalized to project-agnostic examples + explicit `anchor: none`; (4) `ccf-init.md` premortem anchor lacked the "where any exist / anchor none" hedge (it runs on brand-NEW projects with no "Closed" yet) → added, plus the structural-loop "or user accepts" escape to match `ccf-plan`. Premortem = a SECTION inside `ccf-spec-checker` (0 hook change; gate `review-trace.mjs:71` auto-recognizes it; count 6/6/6/1; soft-gate via the step-6 loop, no new hook). **Dogfood:** a SIMULATED spawn (generic agent under the new instructions) returned a correct `🔮 Premortem` block anchored to real "Closed" entries — the REAL `ccf:ccf-spec-checker` spawn needs a plugin reload (the active plugin runs from the v0.3.4 cache, [[ccf-plugin-runs-from-cache-not-repo]]). Reviewed 1× by `ccf-spec-checker` plan-review (2 ❌ + 2 ⚠️ + dogfood premortem → all folded). DEFERRED next iteration (022+, user-requested): subagent access to project skill + project-specific MCP. Memory: [[ground-claude-facts-primary-not-blog]], [[ccf-enforce-with-hook-not-prompt]], [[ccf-plugin-runs-from-cache-not-repo]], [[extend-ccf-review-as-spec-checker-mode]], [[user-delegates-pick-best-practice-not-complex]]. NOT committed (awaiting user).

---

## Origin — output-style-propagation iteration (tasks 017–019) — OPEN
Approved plan `hi-n-t-i-c-c-agent-rustling-wind.md`. User: spawned subagents (esp. `ccf-implementer`) don't follow the output style → code violates coding rules. Root cause (grounded `code.claude.com` + GitHub): output styles modify the MAIN loop's system prompt and are NOT inherited by subagents (subagents get their own system prompt + the memory hierarchy `.claude/rules`/CLAUDE.md, except built-in Explore/Plan); no built-in style→subagent propagation ([#8395](https://github.com/anthropics/claude-code/issues/8395)). Fix = Anthropic's best-practice COMBINATION made deterministic: (1) rules in `.claude/rules` (auto-load); (2) **restate at spawn** via a new `SubagentStart` hook `agent-rules-inject` that injects a coding-rules directive (points at `.claude/rules` + the active output style's coding rules, persona excluded) through `additionalContext` — grounded `code.claude.com/docs/en/hooks` (input field **`agent_type`**, output `additionalContext` "added to the subagent's context… before its first prompt", cannot block); (3) embed the rules in `ccf-implementer`'s body as the prompt backup. Chose SubagentStart over `PreToolUse + updatedInput` (purpose-built, avoids harness-dependent spawn-tool name + the less-certain reach-subagent path). Reviewed 2× by `ccf-spec-checker` (round 1: 2 ❌ + ⚠️ → pivot to SubagentStart + full README sync; round 2: 1 ❌ grounding-condition → satisfied by a cited Context7 query + a BLOCKING grounding step-0). Test discipline OFF (hook lib still `node --test`). Counts: **5 → 6 hooks**. Memory: [[ccf-enforce-with-hook-not-prompt]], [[ground-claude-facts-primary-not-blog]].

## Task backlog — output-style-propagation (in execution order)
| # | Slice | Layers | Gate (tests green) | Depends on | Status |
|---|-------|--------|--------------------|-----------|--------|
| 017 | `agent-rules-inject.mjs` SubagentStart hook injecting coding rules into spawned `ccf-implementer` | hook + 2 lib + test + io.mjs JSDoc + hooks.json | `node --test` + `tsc` + stdin-smoke ≥3 + `validate` + **BLOCKING live-check** before `done` | — | in-review |
| 018 | Harden `ccf-implementer` body (read+obey `.claude/rules` + active output style; self-check before `in-review`) | agent prompt | grep (output-style + self-check wording) + `validate` + re-read | 017 | in-review |
| 019 | Spec + docs + counts sync (5→6 hooks; document the mechanism) | 2 rules + CLAUDE.md + 3 READMEs + plugin README + template | grep-consistency (6 hooks; `agent-rules-inject`/`SubagentStart` present; no "5 hook") + `validate` + `tsc` + `node --test` | 018 | in-review |

> Status: `todo` / `in-progress` / `in-review` / `done` / `blocked`. Lifecycle: `todo → in-progress → in-review → done` — `ccf-implementer` reaches `in-review`; only `/ccf:ccf-updatespec` writes `done` after `/ccf:ccf-check` + `/code-review` (and 017's live-check) pass.
> Per-task detail in `task-017..019-*.md`. Each task runs in a FRESH session via `ccf-implementer`.
> **Artifact-count impact:** +1 hook → **6 cmd / 6 agent / 6 hook / 1 skill**. Sync the count at `CLAUDE.md:17`, `README.md`/`.vi`/`.zh-CN` (count + hook table), `plugins/ccf/README.md` tree. tsconfig NOT touched (glob already covers).

---

## Origin — memory-protocol-align iteration (tasks 015–016)
Approved plan `hi-n-t-i-th-update-splendid-waffle.md`. User asked whether `/ccf:ccf-updatespec` matches the Memory mechanism from a blog post. It mostly does, but is missing/imprecise on **5 points** (all grounded this session via Context7 `/websites/code_claude` + the live system-prompt protocol): (1) MEMORY.md is a pure index capped at **first 200 lines or 25KB, whichever first** — curate if exceeded; (2) body cross-links use `[[name]]` (the `name:` slug), not `[[file-name]]`; (3) memory is point-in-time → describe intent not a code location + verify before asserting; (4) **Auto Memory** (`autoMemoryEnabled`, default on, v2.1.59+) interplay; (5) `feedback` is the strongest type — record wins AND losses, `Why` mandatory. Deliberately NOT adding the blog's unverified "stale after 2 days" / "module #11/22". Reviewed 2× by `ccf-spec-checker` (round 1: 2 ❌ + 3 ⚠️ → all resolved by preserving the per-line cap, grounding+citing the 3 facts, editing feedback in place, making grep the primary gate; round 2: APPROVE). Counts unchanged (content edit only). Memory: [[sync-hook-docs-both-places]].

## Task backlog — memory-protocol-align (in execution order)
| # | Slice | Layers | Gate (tests green) | Depends on | Status |
|---|-------|--------|--------------------|-----------|--------|
| 015 | Align `ccf-updatespec.md` memory instructions (index cap + `[[name]]` + point-in-time + Auto-Memory note + feedback-strongest) | command prompt | grep (`[[file-name]]` gone, `200 lines`+`25KB`+`autoMemoryEnabled` present, `< ~200 chars` kept, feedback flagged) + `claude plugin validate` + re-read | — | done |
| 016 | Propagate the index-cap + feedback-strongest wording to template + 3 READMEs | template + 3 READMEs | language-neutral grep (`200`/`25KB`/`feedback`) consistent across 4 files + `ccf-updatespec.md` + `validate` + cross-lang re-read | 015 | done |

> Status: `todo` / `in-progress` / `in-review` / `done` / `blocked`. Lifecycle: `todo → in-progress → in-review → done` — `ccf-implementer` reaches `in-review`; only `/ccf:ccf-updatespec` writes `done` after `/ccf:ccf-check` + `/code-review` pass.
> Per-task detail in `task-015-*.md` / `task-016-*.md`. Each task runs in a FRESH session via `ccf-implementer`.
> **Artifact-count impact:** NONE — content edit to an existing command + its doc mirrors; no new command/agent/hook/rule. Counts stay **6 cmd / 6 agent / 5 hook / 1 skill**.

## Closed — memory-protocol-align iteration (tasks 015–016)
Both tasks `done` + `/ccf:ccf-check` (0 ❌, 0 drift, scope = 5 files + PLAN.md, counts 6/6/5/1) + `/code-review` xhigh (`[]` — no findings; a pure prose/markdown diff has no surface for the code-correctness angles). Aligned `/ccf:ccf-updatespec`'s memory instructions with Claude Code's real Memory mechanism on 5 grounded points: MEMORY.md is a pure index (first **200 lines or 25KB**, curate when near) while KEEPING the per-line `< ~200 chars` cap; cross-links use `[[name]]` (the `name:` slug); memory is point-in-time (intent-not-location + verify-before-asserting); Auto-Memory (`autoMemoryEnabled`, default on, v2.1.59+) interplay note; `feedback` elevated to the strongest type (wins+losses, mandatory `Why`). Propagated to `tooling.md.tmpl` + 3 READMEs. Grounded via Context7 `/websites/code_claude` + the live system-prompt protocol; deliberately excluded the blog's unverified "2-days-stale" / "module #11/22". Reviewed 2× by `ccf-spec-checker` (round 1: 2 ❌ + 3 ⚠️ → resolved; round 2: APPROVE). Gates green: `claude plugin validate` passed, `node --test` 89/89. Counts unchanged. Memory: [[ground-claude-facts-primary-not-blog]]. NOT committed (awaiting user).

## Origin — context-guard iteration (tasks 012–014)
Approved plan `hi-n-t-i-t-i-th-y-abundant-castle.md`. User: the compact warning is ineffective — over budget yet the model never shows the `/compact` guidance, because `context-nudge.mjs` (PostToolUse) warns only through `additionalContext` (model-facing, ignorable). Fix = replace it with `context-guard.mjs` on `UserPromptSubmit`: default WARN mode surfaces via `systemMessage`(user)+`additionalContext`(model); opt-in HARD-BLOCK mode (`--hard-block` arg) `exit 2`-blocks until `/compact` (escape: `/compact` prefix or `ccf:override`). Grounded in Context7 `/anthropics/claude-code` (systemMessage = universal field; exit 2 = only deterministic block). Reviewed 2× by `ccf-spec-checker` (4 issues → all resolved; 2 minor notes folded). Counts unchanged (replace, not add). Memory: [[ccf-enforce-with-hook-not-prompt]]. Test discipline: OFF (pure helpers still `node --test`'d per repo law).

## Task backlog — context-guard (in execution order)
| # | Slice | Layers | Gate (tests green) | Depends on | Status |
|---|-------|--------|--------------------|-----------|--------|
| 012 | `context-guard.mjs` WARN mode replacing context-nudge (`decideGuardAction` + `emitPromptWarning`, drop `decideNudge`, swap hooks.json wiring) | hook + 2 lib + hooks.json + tsconfig + tests | `node --test` + `tsc` + over/under-threshold stdin smoke + `validate` accepts combined payload + real-session visibility check | — | done |
| 013 | HARD-BLOCK mode + escape hatch (`decideGuardAction` block branch + `--hard-block` argv + `isEscape` + exit 2) | hook + lib + tests | `node --test` (full decision-table) + `tsc` + 3-case stdin smoke (block / escape / no-flag) | 012 | done |
| 014 | Spec + docs sync (hooks.md, CLAUDE.md, READMEs, tsconfig) | spec + docs | `tsc` + `node --test` + `validate` + grep-clean (no `context-nudge`/`decideNudge`) | 013 | done |

> Status: `todo` / `in-progress` / `in-review` / `done` / `blocked`. Lifecycle: `todo → in-progress → in-review → done` — `ccf-implementer` reaches `in-review`; only `/ccf:ccf-updatespec` writes `done` after `/ccf:ccf-check` + `/code-review` pass.
> Per-task detail in `task-012..014-*.md`. Each task runs in a FRESH session via `ccf-implementer`.
> **Artifact-count impact:** NONE — context-nudge is REPLACED by context-guard (still 1 hooks.json entry net; the new one is an additional object in the existing UserPromptSubmit array, the PostToolUse array is removed). Counts stay **6 cmd / 6 agent / 5 hook / 1 skill**.

## Closed — context-guard iteration (tasks 012–014)
All 3 tasks `done` + `/ccf:ccf-check` (0 ❌, 0 drift — 89 lib tests pass, `tsc` exit 0, `claude plugin validate` passed) + `/code-review` low (`(none)` — no runtime bugs). Replaced the soft `PostToolUse` `context-nudge.mjs` (warned only via `additionalContext`, model-ignorable) with `context-guard.mjs` on `UserPromptSubmit`: WARN default surfaces via `emitPromptWarning` (combined `systemMessage`+`additionalContext`); opt-in HARD-BLOCK (`--hard-block` arg) `exit 2`-blocks until `/compact`, escape on `/compact` prefix or `ccf:override`. `decideNudge` + OS-temp dedup removed (per-prompt firing is rate-limited). Counts unchanged (6 cmd / 6 agent / **5 hook** / 1 skill). Memory: [[hook-user-visible-channel]]. **One gate item deferred to the user:** real-session visibility check (confirm `systemMessage` renders as a USER banner at UserPromptSubmit) — needs a live interactive session Claude can't drive. NOT committed (awaiting user).

---

## Origin — plan-status-sync iteration (closed, tasks 010–011)
Approved plan `t-i-mu-n-t-ch-h-p-buzzing-hejlsberg.md`. User found a real process gap: code committed/pushed but the PLAN.md task is still not `done`, and `done` was being set TOO EARLY (by ccf-implementer, before /ccf-check + /code-review — the v0.3.0 session found 4 bugs post-`done`). Fix = (1) add an `in-review` status; `done` set ONLY by `/ccf:ccf-updatespec` after a clean review; (2) a 3rd advisory clause in the existing Stop hook that nudges when the session committed but PLAN.md has tasks not `done`. Reviewed 3× by `ccf-spec-checker` (3 ❌ + 3 ⚠️ → all resolved). Memory: [[plan-status-sync-on-commit]].

## Task backlog (in execution order)
| # | Slice | Layers | Gate (tests green) | Depends on | Status |
|---|-------|--------|--------------------|-----------|--------|
| 010 | `in-review` status: rename `findInProgressTask`→`findActiveTask` (+widen for in-review) + prompt lifecycle (done only after review) + templates + repo legend | hook lib + agent + 3 commands + templates + rule | `node --test plan.test.mjs` + `tsc` + grep-consistency + session-start reload smoke | — | done |
| 011 | Stop-hook clause C: nudge committed-this-session + tasks not done (`findNonDoneTasks` in plan.mjs + new `git-trace.mjs` `committedThisSession` + tests + doc sync) | hook + 2 lib + test + docs | `node --test` (both helpers) + `tsc` + 4-case stdin smoke + `validate` green | 010 | done |

> Status: `todo` / `in-progress` / `in-review` / `done` / `blocked`. Lifecycle: `todo → in-progress → in-review → done` — `ccf-implementer` reaches `in-review`; only `/ccf:ccf-updatespec` writes `done` after `/ccf:ccf-check` + `/code-review` pass.
> Per-task detail in `task-NNN-*.md`. Each task runs in a FRESH session via `ccf-implementer`.
> **Artifact-count impact:** NONE — no new command/agent/HOOK ENTRY (`git-trace.mjs` is a lib, not a hooks.json entry; clause C composes into the existing `updatespec-nudge.mjs`). Counts stay **6 cmd / 6 agent / 5 hook / 1 skill**. NOTE: task 010 adds the `in-review` value to this file's own status legend (dogfood) — lines 6 + 20.

## Closed — plan-status-sync iteration (tasks 010–011)
Both tasks `done` + `/ccf:ccf-check` (8/8 dimensions pass, 0 ❌) + `/code-review` high (0 correctness bugs — clean first pass; only a documented duplicate-transcript-read note added). `in-review` status shipped: `ccf-implementer` reaches `in-review`, only `/ccf:ccf-updatespec` writes `done` after check+review pass (this very close is the first dogfood of that lifecycle). Stop-hook clause C nudges when the session committed but PLAN.md has non-done tasks (`committedThisSession` role-gated in `git-trace.mjs`, `findNonDoneTasks` in `plan.mjs`). Gates green: 88 lib unit tests, `tsc` exit 0, `claude plugin validate` passed, 4-case stdin smoke verified. Counts unchanged (6 cmd / 6 agent / 5 hook / 1 skill). NOT committed (awaiting user).

## Closed — test-discipline iteration (tasks 005–009, v0.3.0)
All 5 tasks `done` + `/ccf:ccf-check` (1 ❌ count-drift fixed: CLAUDE.md/architecture.md/plugin-README/MEMORY 5→6 commands) + `/code-review` high (4 CONFIRMED + 1 PLAUSIBLE fixed): ccf-init now instantiates the hook lib verbatim + strips `.tmpl`; test-gate transcript scan role-gated (no false block on user prose); `FAIL_SIGNAL` no longer matches "0 failed" (extracted to tested `isFailureText`). Gates green: 70 repo + 8 template unit tests, `tsc` exit 0, `claude plugin validate` passed, 5-case hook smoke (block/allow/loop-guard) verified. `discipline: on` free-text marker kept (LLM-semantic + gate-names-matrix fallback; structured field = YAGNI). NOT committed (awaiting user).

## Closed — previous iteration (best-practice integration, v0.2.0)
4 tasks (001 prompt-hygiene, 002 path-scoped rules docs, 003 verify-work Stop nudge, 004 settings.json/attribution) all done + `/ccf:ccf-check` (0 violations) + `/code-review` (4 findings fixed: PowerShell/`pwsh` in SHELL_TOOL_NAMES, MultiEdit shape tolerance via `editedAnyCodeFile`, hooks.md + README verify-work clause sync). Gates green: 70 unit tests, `tsc` exit 0, `claude plugin validate` passed. Detail: `task-001..004-*.md`.
